---
type: quick
task_id: 260325-26a
date: "2026-03-25"
duration: ~5 min
files_modified:
  - src/components/dashboard/Dashboard.tsx
commits:
  - fb68f40
---

# Quick Task 260325-26a: Fix Three Dashboard UI Issues

One-liner: Removed green top-border from focus cards, fixed pod card hover clipping, added right-edge scroll fade to pod section.

## Changes

### Task 1 — Focus card border removed
- Dropped `border: '1px solid var(--divider)'` and `borderTop: '2px solid var(--color-brand)'`
- Replaced with `background: 'rgba(37,180,57,0.04)'` (faint green tint) and `boxShadow: '0 1px 3px var(--divider)'`
- Fixed mouseleave handler to restore rest shadow instead of dropping to `none`

### Task 2 — Pod card hover clipping fixed
- Added `paddingTop: 6, paddingBottom: 6` to the pod cards scroll container
- The 2px translateY lift on hover is now fully visible without clipping at the container top edge

### Task 3 — Scroll fade indicator added
- Wrapped pod cards scroll div in a relative-positioned outer container
- Added an absolute-positioned right-edge fade overlay: `linear-gradient(to right, transparent, var(--color-bg))`
- Hidden scrollbar via `scrollbarWidth: 'none'`
- `pointerEvents: 'none'` on the fade overlay so it doesn't block scroll or clicks

## Deviations

None — plan executed exactly as written.

## Self-Check: PASSED

- [x] `src/components/dashboard/Dashboard.tsx` modified
- [x] Build passes clean (`tsc -b && vite build`)
- [x] Commit `fb68f40` exists
