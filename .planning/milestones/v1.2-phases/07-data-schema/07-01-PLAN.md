---
phase: 07-data-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "Contacts table has all new fields: First Name, Last Name, LinkedIn, City, Country, Global Region, Gender, Introduced By, Intel / Notes, Relationship Owner, Contact Frequency, Next Follow-Up Date, Next Action, KV Fund Investor, SPV Investor, Needs Review"
    - "Interactions table has new fields: Summary, Source, Email Link, Granola Link"
    - "All 6 pods exist as records in the Lists table: LPs, Maps, Maps Lite, Talent, Service Providers, Unsorted"
  artifacts:
    - path: "Airtable Contacts table schema"
      provides: "Expanded contact fields"
    - path: "Airtable Interactions table schema"
      provides: "Expanded interaction fields"
    - path: "Airtable Lists table records"
      provides: "6 pod records"
  key_links:
    - from: "Contacts table"
      to: "Lists table"
      via: "Lists linked record field"
      pattern: "Each contact links to a pod via Lists field"
---

<objective>
Expand Airtable schema and create pod records for the V1 data model.

Purpose: The contact and interaction tables need new fields before any data can be imported. Pods must exist as records so contacts can link to them.
Output: Airtable schema updated, 6 pod records created.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/types.ts
@src/lib/airtable.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Contacts and Interactions table schemas</name>
  <files></files>
  <read_first>src/lib/types.ts, src/lib/airtable.ts</read_first>
  <action>
Use Airtable MCP tools to add new fields to the Contacts table (base ID from env: appXXX, table ID: tbll75mRMMVBGiNpj).

First, call list_tables_for_base to get the current schema and field IDs.

Then add these new fields to the Contacts table:
- "First Name" — single line text
- "Last Name" — single line text
- "LinkedIn" — url
- "Country" — single select (options: USA, UK, France, UAE, India, Canada, Brazil, Japan, Germany, Australia, Mexico, Colombia, Senegal, Sweden, Other)
- "Global Region" — single select (options: AMER, APAC, ME, LATAM, EU)
- "Gender" — single select (options: Male, Female, Non-binary, Other)
- "Introduced By" — single line text
- "Intel / Notes" — long text (this is the primary notes field from spec)
- "Relationship Owner" — single line text
- "Contact Frequency" — single select (options: Weekly, Monthly, Quarterly, Annual, As Needed)
- "Next Follow-Up Date" — date
- "Next Action" — single line text
- "KV Fund Investor" — multiple select (options: Fund I, Fund II)
- "SPV Investor" — multiple select (options: Moonpay, OpenAI, Lovable, Carta, TeraWulf, Republic Realm, Clubhouse, Starface, Olipop, Applyboard)
- "Needs Review" — checkbox

Note: "City" already exists as "Location" — keep using Location field, no rename needed.
Note: Do NOT delete the existing "Name" field. Keep it as-is. First Name / Last Name are additive.
Note: The existing "Notes" field stays. "Intel / Notes" is the new spec field — it's a separate long text field.

Then add these new fields to the Interactions table (table ID: tblbxLX5EM09Y4xim):
- "Summary" — long text (the spec's primary summary field; existing "Notes" field stays)
- "Source" — single select (options: Gmail, Granola, Manual)
- "Email Link" — url
- "Granola Link" — url

Use the Airtable MCP create_field tool for each field. If that tool isn't available, use the Airtable REST API via the airtable MCP tools to create fields.
  </action>
  <verify>
    <automated>Use list_tables_for_base or get_table_schema via Airtable MCP to confirm all new fields exist on both tables.</automated>
  </verify>
  <acceptance_criteria>All 16 new Contact fields and 4 new Interaction fields are visible in Airtable schema.</acceptance_criteria>
  <done>Contacts table has 16 new fields, Interactions table has 4 new fields. All field types correct.</done>
</task>

<task type="auto">
  <name>Task 2: Create pod records in Lists table</name>
  <files></files>
  <read_first>src/lib/airtable.ts</read_first>
  <action>
Using Airtable MCP tools, first list existing records in the Lists table (tblnsxNUscKApvMsV) to see what pods already exist.

Create missing pod records. The 6 required pods with their properties:

1. "LPs" — Color: #FF6B8A, Owner: moj_mahdara, Is Priority: true, Cadence: monthly
2. "Maps" — Color: #E53935, Owner: moj_mahdara, Is Priority: true, Cadence: biweekly
3. "Maps Lite" — Color: #FF7043, Owner: moj_mahdara, Is Priority: false, Cadence: monthly
4. "Talent" — Color: #25B439, Owner: moj_mahdara, Is Priority: false, Cadence: quarterly
5. "Service Providers" — already exists, verify it's there. If missing, create with Color: #F5A623, Owner: moj_mahdara, Is Priority: false, Cadence: monthly
6. "Unsorted" — Color: #9E9E9E, Owner: moj_mahdara, Is Priority: false, Cadence: monthly

First call get_table_schema to get the field IDs for Name, Color, Owner, Is Priority, Cadence. Then use create_record for each missing pod.

Record the created record IDs — they'll be needed in Plan 02 to link contacts to pods.
  </action>
  <verify>
    <automated>Use list_records_for_table on the Lists table to confirm 6 pod records exist with correct names.</automated>
  </verify>
  <acceptance_criteria>Lists table contains exactly these 6 pods: LPs, Maps, Maps Lite, Talent, Service Providers, Unsorted. Each has Color, Owner, Is Priority, and Cadence set.</acceptance_criteria>
  <done>6 pod records exist in Airtable Lists table with correct field values.</done>
</task>

</tasks>

<verification>
- list_tables_for_base confirms new fields on Contacts and Interactions tables
- list_records_for_table on Lists table returns 6 records matching expected pod names
</verification>

<success_criteria>
Airtable schema is fully expanded. 6 pods exist. Ready for contact/interaction import in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-schema/07-01-SUMMARY.md`
Include the record IDs of all 6 pods — Plan 02 needs them to link contacts.
</output>
