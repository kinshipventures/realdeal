---
phase: 09-add-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/contacts/AddContactModal.tsx
  - src/components/dashboard/Dashboard.tsx
  - src/App.tsx
  - src/lib/sampleData.ts
  - src/lib/airtable.ts
autonomous: false
requirements:
  - ADD-01
  - ADD-02

must_haves:
  truths:
    - "User can open an Add Contact modal from the dashboard"
    - "Structured form validates required fields (First Name, Last Name, Email, Pod) before save"
    - "Brain dump path accepts free text and creates contact in Unsorted pod with Needs Review = true"
    - "Newly created contact appears in the correct pod without page refresh"
  artifacts:
    - path: "src/components/contacts/AddContactModal.tsx"
      provides: "Two-tab modal: structured entry + brain dump"
      min_lines: 150
    - path: "src/components/dashboard/Dashboard.tsx"
      provides: "FAB trigger for Add Contact"
    - path: "src/App.tsx"
      provides: "Modal state management and cache invalidation"
  key_links:
    - from: "src/components/contacts/AddContactModal.tsx"
      to: "src/lib/airtable.ts"
      via: "createContact() call"
      pattern: "createContact"
    - from: "src/App.tsx"
      to: "src/components/contacts/AddContactModal.tsx"
      via: "state toggle and onCreated callback"
      pattern: "AddContactModal"
---

<objective>
Add Contact feature: structured entry form and brain dump path accessible from the dashboard.

Purpose: Moj or Briell can add a new contact without leaving the app — either via a structured form with validation or a quick brain dump that drops into Unsorted for later triage.
Output: AddContactModal component, FAB on dashboard, wired to createContact in airtable.ts with demo mode support.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/types.ts
@src/lib/airtable.ts
@src/components/campaigns/CampaignCreate.tsx
@src/App.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/types.ts:
```typescript
export interface Contact {
  id: string
  name: string
  email: string | null
  phone: string | null
  company: string | null
  role: string | null
  first_name: string | null
  last_name: string | null
  intel_notes: string | null
  list_ids: string[]        // linked Pod record IDs
  category_ids: string[]
  needs_review: boolean
  // ... other fields
}

export interface Pod {
  id: string
  name: string
  color: HexColor | null
  // ...
}
```

From src/lib/airtable.ts:
```typescript
export async function createContact(data: Omit<Contact, 'id' | 'created_at'>): Promise<Contact>
export async function getPods(): Promise<Pod[]>
export function invalidateContactsCache(): void
```

Unsorted pod record ID (Airtable): recGR6AQTq1ceL1yq
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AddContactModal component with structured + brain dump modes</name>
  <files>src/components/contacts/AddContactModal.tsx, src/lib/airtable.ts, src/lib/sampleData.ts</files>
  <read_first>
    - src/lib/airtable.ts (createContact function, lines 327-370)
    - src/lib/types.ts (Contact, Pod types)
    - src/components/campaigns/CampaignCreate.tsx (UI pattern reference)
    - src/components/campaigns/CampaignDetail.tsx (slide-out panel pattern)
    - src/lib/sampleData.ts (demo mode patterns, DEMO_CONTACTS array)
    - src/lib/escapeStack.ts (useEscape hook)
  </read_first>
  <action>
Create `src/components/contacts/AddContactModal.tsx` — a centered modal overlay (not slide-out) with two modes toggled by pill tabs at the top: "Add Contact" (structured) and "Quick Note" (brain dump).

**Modal chrome:**
- Backdrop: rgba(0,0,0,0.3) with backdrop-filter blur(8px), click to close
- Panel: var(--surface-panel) background, var(--panel-blur) backdrop, border-radius 16px, max-width 440px, centered
- Use useEscape from escapeStack for Escape key dismiss
- Auto-focus first name field on open (structured) or textarea (brain dump)

**Structured mode (ADD-01):**
- Required fields: First Name, Last Name, Email, Pod (multi-select from existing pods, exclude any pod named "Unsorted")
- Optional fields: Job Title (maps to `role`), Company, Phone, Intel/Notes
- Pod picker: load pods via getPods(), render as toggle chips (similar to CampaignCreate type selector pattern). User must select at least one. Exclude pods where `pod.name === 'Unsorted'`.
- Save button disabled until all required fields filled + at least one pod selected
- On save: call createContact() with `name: "${firstName} ${lastName}"`, `first_name`, `last_name`, `email`, `list_ids: [selected pod IDs]`, `role`, `company`, `phone`, `intel_notes`, `needs_review: false`
- On success: call onCreated(contact) prop, close modal
- On error: inline error text above save button ("failed to save — try again")

**Brain dump mode (ADD-02):**
- Single large textarea, placeholder: "Sarah Russell, Eight Sleep, met at Upfront, intro via Doug..."
- Save button always enabled when textarea has content (no validation)
- On save: call createContact() with `name: "Brain Dump"`, `intel_notes: textareaValue`, `list_ids: ['recGR6AQTq1ceL1yq']` (Unsorted pod ID), `needs_review: true`, all other fields null/empty
- Same success/error pattern as structured

**Demo mode support in airtable.ts createContact:**
The existing createContact (line 327) does NOT have a demo mode guard. Add one at the top of createContact: if isDemoMode(), create a fake Contact object with `id: 'demo-contact-${Date.now()}'`, push it to DEMO_CONTACTS, and return it. Follow the same pattern as createCampaign's demo guard (line 714).

**Demo mode support in sampleData.ts:**
Ensure DEMO_CONTACTS is exported as `let` (mutable) so demo createContact can push to it. Check current export — if it's `const`, the in-place push will work on arrays since const allows mutation of array contents.

**Styling:**
- Follow existing input styling from CampaignCreate: var(--tint) background, 1px solid var(--edge-strong) border, border-radius 7px, fontSize 13
- Labels: fontSize 11, fontWeight 500, color var(--color-text-secondary), marginBottom 4
- Field groups: marginBottom 12
- Pod chips: similar to CampaignCreate type buttons — pill shape, border highlight when selected, multiple can be active
- Tab pills at top: two options in a pill group, var(--color-brand) background for active tab with white text
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/kinshipbrain && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AddContactModal renders with two tab modes
    - Structured mode has required field validation (save disabled until First Name, Last Name, Email, and at least one pod filled)
    - Brain dump mode has single textarea, creates contact in Unsorted with needs_review=true
    - Demo mode createContact works without hitting Airtable API
    - All existing type-check passes
  </acceptance_criteria>
  <done>AddContactModal component exists with both modes, createContact has demo guard, builds clean</done>
</task>

<task type="auto">
  <name>Task 2: Wire modal into App shell with FAB trigger on dashboard</name>
  <files>src/App.tsx, src/components/dashboard/Dashboard.tsx</files>
  <read_first>
    - src/App.tsx (full file — shell structure, state management)
    - src/components/dashboard/Dashboard.tsx (first 50 lines for props/structure)
  </read_first>
  <action>
**App.tsx changes:**
- Import AddContactModal
- Add state: `const [showAddContact, setShowAddContact] = useState(false)`
- Add `onAddContact` prop to Dashboard route: `<Dashboard onAddContact={() => setShowAddContact(true)} />`
- Render AddContactModal when showAddContact is true, after the search palette block
- onCreated callback: close modal, invalidateContactsCache(), trigger a reload. Since Dashboard manages its own data fetching, the simplest approach is `window.location.reload()` after create — same pattern used by demo toggle. If Dashboard exposes a refresh mechanism, use that instead.
- onClose: setShowAddContact(false)

**Dashboard.tsx changes:**
- Accept new prop: `onAddContact?: () => void`
- Add a floating action button (FAB) in the bottom-right corner of the dashboard, positioned above the nav bar
- FAB style: 48px circle, var(--color-brand) background, white "+" icon (SVG or text), box-shadow for depth, position fixed, bottom 80px (above nav pill), right 24px, z-index 90
- On mobile: bottom 112px (above mobile tab bar)
- onClick: call onAddContact()
- The FAB should only appear on the dashboard (it's rendered inside Dashboard, so it naturally scopes)
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/kinshipbrain && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - FAB visible on dashboard, not on map or import views
    - Clicking FAB opens AddContactModal
    - Creating a contact via structured form saves and refreshes data
    - Creating a contact via brain dump saves to Unsorted with Needs Review
    - Escape closes modal, backdrop click closes modal
  </acceptance_criteria>
  <done>FAB on dashboard opens AddContactModal, full create flow works in both modes, cache invalidated on save</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Add Contact flow</name>
  <what-built>Add Contact modal with structured entry and brain dump modes, triggered by FAB on dashboard</what-built>
  <how-to-verify>
    1. Open http://localhost:5173 (or production URL) with demo mode ON
    2. Confirm green "+" FAB visible in bottom-right of dashboard
    3. Click FAB — modal should open centered with "Add Contact" tab active
    4. Try clicking Save with empty fields — should be disabled
    5. Fill in First Name, Last Name, Email, select a pod — Save should enable
    6. Submit — modal closes, check that contact appears (may need to scroll to pod)
    7. Open modal again, switch to "Quick Note" tab
    8. Type some text, click Save — should create and close
    9. Press Escape or click backdrop — modal should close without saving
    10. Test on mobile viewport (resize to <768px) — FAB should be above the tab bar
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` passes with no type errors
- AddContactModal renders both modes
- createContact demo guard prevents API calls in demo mode
- FAB only visible on dashboard view
</verification>

<success_criteria>
- Structured form blocks save until required fields filled (ADD-01)
- Brain dump creates contact in Unsorted with Needs Review = true (ADD-02)
- Both paths work in demo mode and live mode
- Contact appears in correct pod after creation
</success_criteria>

<output>
After completion, create `.planning/phases/09-add-contact/09-01-SUMMARY.md`
</output>
