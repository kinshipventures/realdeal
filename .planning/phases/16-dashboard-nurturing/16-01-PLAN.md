---
phase: 16-dashboard-nurturing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/snooze.ts
  - src/components/dashboard/useDashboardConfig.ts
  - src/components/dashboard/DashboardSettings.tsx
  - src/components/dashboard/widgets/EquityWidget.tsx
  - src/components/dashboard/widgets/PodHealthWidget.tsx
  - src/components/dashboard/widgets/WrappedWidget.tsx
  - src/components/dashboard/widgets/TodaysFocusWidget.tsx
  - src/components/dashboard/widgets/NeedsAttentionWidget.tsx
  - src/components/dashboard/widgets/ComingUpWidget.tsx
  - src/components/dashboard/widgets/RecentActivityWidget.tsx
  - src/components/dashboard/widgets/QuickLinksWidget.tsx
  - src/components/dashboard/Dashboard.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "User sees all existing dashboard sections rendered identically to before (no visual regression)"
    - "User can open a settings panel and toggle individual widgets on/off"
    - "User can switch between Full and Focus presets and the visible widgets change immediately"
    - "Widget visibility persists across page refresh via localStorage"
    - "Overdue queue and dormant cleanup are merged into a single Needs Attention section grouped by severity"
    - "Campaigns and pipelines sections are replaced with compact navigational link cards"
  artifacts:
    - path: "src/lib/snooze.ts"
      provides: "Extracted snooze utilities shared by dashboard and nurturing hub"
      exports: ["getSnoozedIds", "snoozeContact"]
    - path: "src/components/dashboard/useDashboardConfig.ts"
      provides: "Widget config hook with localStorage persistence"
      exports: ["useDashboardConfig", "WidgetId", "Preset", "PRESET_CONFIGS"]
    - path: "src/components/dashboard/DashboardSettings.tsx"
      provides: "Settings panel overlay with preset switcher and toggle list"
      exports: ["DashboardSettings"]
    - path: "src/components/dashboard/Dashboard.tsx"
      provides: "Orchestrator that fetches data and renders visible widgets"
  key_links:
    - from: "src/components/dashboard/Dashboard.tsx"
      to: "src/components/dashboard/useDashboardConfig.ts"
      via: "useDashboardConfig() hook call"
      pattern: "useDashboardConfig\\("
    - from: "src/components/dashboard/Dashboard.tsx"
      to: "src/lib/snooze.ts"
      via: "import getSnoozedIds, snoozeContact"
      pattern: "from.*snooze"
    - from: "src/components/dashboard/DashboardSettings.tsx"
      to: "src/components/dashboard/useDashboardConfig.ts"
      via: "receives config + setters as props"
      pattern: "toggleWidget|applyPreset"
---

<objective>
Decompose Dashboard.tsx into a widget-based architecture with localStorage-backed configuration, preset system, and settings panel.

Purpose: Transform the 500-line monolithic dashboard into toggleable widgets so users can customize their daily operating surface. This is the foundation for the entire phase -- nurturing hub and signal propagation depend on this widget architecture.

Output: Widget components, config hook, settings panel, snooze extraction. Dashboard renders identically by default but now supports show/hide and presets.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dashboard-nurturing/16-CONTEXT.md
@.planning/phases/16-dashboard-nurturing/16-RESEARCH.md

@src/components/dashboard/Dashboard.tsx
@src/components/dashboard/WrappedCard.tsx
@src/components/categorization/PendingTrayWidget.tsx
@src/lib/equity.ts
@src/lib/birthdays.ts
@src/lib/escapeStack.ts
@src/lib/airtable.ts
@src/lib/types.ts
@src/App.tsx
@docs/design-system.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract snooze utilities and create widget config hook</name>
  <files>src/lib/snooze.ts, src/components/dashboard/useDashboardConfig.ts</files>
  <read_first>
    - src/components/dashboard/Dashboard.tsx (lines 37-59 for snooze functions, full file for widget section inventory)
    - src/lib/escapeStack.ts (pattern reference for module-level utility)
    - docs/design-system.md (design tokens)
  </read_first>
  <action>
**1. Create `src/lib/snooze.ts`** — Extract verbatim from Dashboard.tsx lines 37-59:

```typescript
const SNOOZE_KEY = 'kinshipbrain:dormant-snooze'

export function getSnoozedIds(): Set<string> {
  try {
    const raw = JSON.parse(localStorage.getItem(SNOOZE_KEY) ?? '{}') as Record<string, number>
    const now = Date.now()
    const active = new Set<string>()
    const cleaned: Record<string, number> = {}
    for (const [id, until] of Object.entries(raw)) {
      if (until > now) { active.add(id); cleaned[id] = until }
    }
    localStorage.setItem(SNOOZE_KEY, JSON.stringify(cleaned))
    return active
  } catch { return new Set() }
}

export function snoozeContact(id: string) {
  try {
    const raw = JSON.parse(localStorage.getItem(SNOOZE_KEY) ?? '{}')
    raw[id] = Date.now() + 30 * 24 * 60 * 60 * 1000
    localStorage.setItem(SNOOZE_KEY, JSON.stringify(raw))
  } catch { /* silent */ }
}
```

**2. Create `src/components/dashboard/useDashboardConfig.ts`** — localStorage-backed config hook:

- Storage key: `kinshipbrain:dashboard-config:v1`
- WidgetId type: `'equity' | 'wrapped' | 'pod-health' | 'todays-focus' | 'needs-attention' | 'coming-up' | 'recent-activity' | 'quick-links' | 'pending-tray'`
- Preset type: `'full' | 'focus'`
- PRESET_CONFIGS map:
  - `full`: all 9 widget IDs visible
  - `focus`: only `['pending-tray', 'todays-focus', 'needs-attention', 'coming-up', 'quick-links']` (per D-02)
- ALL_WIDGETS array with `{ id: WidgetId, label: string }` for the settings toggle list — labels: "Equity Ring", "Wrapped Insights", "Pod Health", "Today's Focus", "Needs Attention", "Coming Up", "Recent Activity", "Quick Links", "Pending Tray"
- DashboardConfig interface: `{ preset: Preset; visible: Set<WidgetId> }`
- Hook returns: `{ config: DashboardConfig; isVisible: (id: WidgetId) => boolean; toggleWidget: (id: WidgetId) => void; applyPreset: (preset: Preset) => void }`
- Read config synchronously from localStorage on mount. If stored config is missing widget IDs that exist in ALL_WIDGETS, fill gaps from the current preset's defaults (per Research Pitfall 2 — version drift protection).
- Write config to localStorage on every change (no confirm step per D-01).
- When `applyPreset` is called, overwrite the visible set with that preset's config and store the preset name. The stored `visible` set is always authoritative (per Research anti-pattern note).
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/mrm && npx tsc --noEmit src/lib/snooze.ts src/components/dashboard/useDashboardConfig.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/snooze.ts contains `export function getSnoozedIds()` and `export function snoozeContact(`
    - src/lib/snooze.ts contains `kinshipbrain:dormant-snooze`
    - src/components/dashboard/useDashboardConfig.ts contains `export function useDashboardConfig(`
    - src/components/dashboard/useDashboardConfig.ts contains `kinshipbrain:dashboard-config:v1`
    - src/components/dashboard/useDashboardConfig.ts contains `type WidgetId` or `export type WidgetId`
    - src/components/dashboard/useDashboardConfig.ts contains `PRESET_CONFIGS`
    - src/components/dashboard/useDashboardConfig.ts contains `focus` preset with exactly 5 widget IDs: pending-tray, todays-focus, needs-attention, coming-up, quick-links
  </acceptance_criteria>
  <done>Snooze utilities extracted to shared module. Dashboard config hook created with localStorage persistence, two presets (Full/Focus), and version-drift-safe loading.</done>
</task>

<task type="auto">
  <name>Task 2: Decompose dashboard into widget components and wire settings panel</name>
  <files>
    src/components/dashboard/widgets/EquityWidget.tsx,
    src/components/dashboard/widgets/PodHealthWidget.tsx,
    src/components/dashboard/widgets/WrappedWidget.tsx,
    src/components/dashboard/widgets/TodaysFocusWidget.tsx,
    src/components/dashboard/widgets/NeedsAttentionWidget.tsx,
    src/components/dashboard/widgets/ComingUpWidget.tsx,
    src/components/dashboard/widgets/RecentActivityWidget.tsx,
    src/components/dashboard/widgets/QuickLinksWidget.tsx,
    src/components/dashboard/DashboardSettings.tsx,
    src/components/dashboard/Dashboard.tsx
  </files>
  <read_first>
    - src/components/dashboard/Dashboard.tsx (full file — every section being extracted)
    - src/components/dashboard/useDashboardConfig.ts (Task 1 output — config hook API)
    - src/lib/snooze.ts (Task 1 output — imported by Dashboard.tsx)
    - src/components/categorization/PendingTrayWidget.tsx (widget pattern reference — section header with count badge and "See all")
    - src/components/dashboard/WrappedCard.tsx (wrapped into WrappedWidget)
    - src/lib/equity.ts (todaysFocus, isDormant, isOverdue, scoreLabel, indexByContact, podEquityScore, overallEquityScore, daysSinceContact, contactCadenceDays)
    - src/lib/airtable.ts (isOverdue, isInGracePeriod, getPipelines, getCampaigns imports)
    - src/lib/types.ts (Contact, Pod, Interaction, FocusItem, Campaign, CampaignContact, Opportunity, Pipeline)
    - src/App.tsx (route structure)
    - docs/design-system.md (design tokens, colors, spacing)
  </read_first>
  <action>
**Create `src/components/dashboard/widgets/` directory** and extract each dashboard section into its own component. Every widget receives data via props (NO internal fetching). Dashboard.tsx keeps all `useMemo` hooks and `useEffect` fetches.

**PANEL constant** — copy to each widget file that needs it:
```typescript
const PANEL: React.CSSProperties = {
  background: 'var(--surface-panel)',
  backdropFilter: 'var(--panel-blur)',
  WebkitBackdropFilter: 'var(--panel-blur)',
  border: 'var(--surface-panel-border)',
  borderRadius: 'var(--panel-radius)',
}
```

**Widget components** (each follows the same shape — props-only, no fetching):

1. **EquityWidget.tsx** — Extract the green header band: equity ring + stats grid. Props: `{ overallScore: number; podCount: number; contactCount: number; recentlyContacted: number; overdueCount: number; interactionsLoading: boolean; dataReady: boolean }`. Contains the `EquityRing` and `StatBlock` sub-components (move from Dashboard.tsx). Note: this widget is special — it renders in the green header band above the main content area, not in the scrollable widget list.

2. **PodHealthWidget.tsx** — Extract pod health cards horizontal scroll. Props: `{ podStats: Array<{ pod: Pod; contactCount: number; overdueCount: number; score: number; scoreReady: boolean; sparkline: number[] | null }>; dataReady: boolean }`. Contains the `PodCard` sub-component (move from Dashboard.tsx). Include the right-fade gradient overlay.

3. **WrappedWidget.tsx** — Thin wrapper around existing `WrappedCard`. Props: `{ insights: WrappedInsight[]; loading: boolean }`. Renders `<WrappedCard insights={insights} />` when not loading and insights exist. Returns null otherwise.

4. **TodaysFocusWidget.tsx** — Extract Today's Focus cards. Props: `{ items: FocusItem[]; onContactClick: (contact: Contact) => void }`. Contains the `FocusCard` sub-component (move from Dashboard.tsx). Add "See all" link in section header that navigates to `/pulse/nurturing?filter=focus`. Use `useNavigate()` from react-router.

5. **NeedsAttentionWidget.tsx** — **Merge overdue queue + dormant cleanup into one section** (per D-04). Props: `{ overdueContacts: Array<{ contact: Contact; days: number | null; podName: string; overdueDays: number }>; dormantContacts: Array<{ contact: Contact; days: number | null }>; contactsLoading: boolean; error: string | null; onContactClick: (contact: Contact) => void; onSnooze: (id: string) => void; onRemoveContact: (id: string) => Promise<void> }`. Render overdue group first (with orange count badge in header), then dormant group below (with "gone quiet" subheader, collapsible). The overdue rows come from existing `OverdueRow` component (move from Dashboard.tsx). Dormant rows use existing `DormantRow` (move from Dashboard.tsx). Add "See all" link navigating to `/pulse/nurturing?filter=overdue`. Section heading: "needs attention". Keep the existing `confirmDeleteId` state for dormant cleanup delete confirmation.

6. **ComingUpWidget.tsx** — Extract Coming Up section (birthdays + follow-ups). Props: `{ items: Array<{ type: 'birthday' | 'follow-up'; contact: Contact; pod: Pod | null; daysUntil: number; label: string; sublabel: string }>; onContactClick: (contact: Contact) => void }`. Contains the `UpcomingRow` sub-component (move from Dashboard.tsx). Add "See all" link navigating to `/pulse/nurturing?filter=dates`.

7. **RecentActivityWidget.tsx** — Extract Recent Activity list. Props: `{ items: Array<{ interaction: Interaction; contact: Contact }>; onContactClick: (contact: Contact) => void }`. Contains the `RecentActivityRow` sub-component (move from Dashboard.tsx).

8. **QuickLinksWidget.tsx** — **New component replacing verbose campaigns section** (per D-06/D-16). Props: `{ campaigns: Campaign[]; campaignContacts: CampaignContact[]; pipelines: Pipeline[]; campaignsLoading: boolean; pipelinesLoading: boolean }`. Renders compact navigational link cards at the bottom of the dashboard. Each card: name on the left, brief status on the right (for campaigns: "X/Y contacted" from campaignContacts; for pipelines: opportunity count or just the name), click navigates to `/pipelines` or opens campaign detail. Style: small cards with minimal info, similar to Linear's quick access. Use pod colors from `POD_SHIFT_COLORS` for pipeline accent if available, otherwise neutral. Campaigns show active ones only. Section heading: "quick links". Fetch pipelines via `getPipelines()` — this is the ONE exception to "no internal fetching" rule since pipelines are not currently loaded by Dashboard.tsx. Use a `useEffect` inside QuickLinksWidget to fetch pipelines on mount (they're lightweight — just names and IDs).

**Create `src/components/dashboard/DashboardSettings.tsx`** — Settings panel overlay:
- Props: `{ config: DashboardConfig; onToggle: (id: WidgetId) => void; onPreset: (preset: Preset) => void; onClose: () => void }`
- Rendered as a panel sliding in from the right (like ContactDetail pattern) or a centered modal. Use `useEscape()` from escapeStack for close-on-escape.
- Top section: two preset buttons ("Full" and "Focus") — the active one is highlighted. Clicking applies the preset immediately.
- Below: toggle list showing all widgets from `ALL_WIDGETS`. Each row: widget label + toggle switch. Toggling updates immediately.
- Style: same PANEL background, 320px width if slide-in, centered if modal. Blur backdrop.

**Refactor `src/components/dashboard/Dashboard.tsx`**:
- Remove `getSnoozedIds` and `snoozeContact` functions — import from `src/lib/snooze.ts`
- Remove all extracted sub-components (EquityRing, StatBlock, PodCard, FocusCard, OverdueRow, DormantRow, UpcomingRow, RecentActivityRow, CampaignCard, DashboardSkeleton)
- Keep all `useState`, `useEffect`, and `useMemo` hooks — Dashboard.tsx remains the data orchestrator
- Add `useDashboardConfig()` hook call at top of component
- Add state for settings panel: `const [showSettings, setShowSettings] = useState(false)`
- Add a gear icon button in the green header band (top-right area) that opens the settings panel
- Add state for pipelines: `const [pipelines, setPipelines] = useState<Pipeline[]>([])` and fetch via `useEffect` with `getPipelines()`
- Render widgets conditionally based on `config.isVisible(widgetId)`:
  - EquityWidget always renders (it's in the header band — per D-05 it's toggleable but in Full preset only)
  - Actually: wrap EquityWidget in `isVisible('equity')` check too. When hidden, the green header band still renders but just shows a simplified version or just the stats.
  - Each widget section: `{isVisible('pod-health') && <PodHealthWidget ... />}`
- Widget render order in the scrollable area: pending-tray, pod-health, wrapped, recent-activity, coming-up, todays-focus, needs-attention, quick-links
- Remove the verbose campaigns section entirely — replaced by QuickLinksWidget
- Remove separate dormant cleanup section — merged into NeedsAttentionWidget
- Keep `selectedContact` / `selectedCampaignId` overlays (ContactDetail, CampaignDetail) and `showQueue` (CategorizationQueue)
- Keep the `DashboardSkeleton` as a simple inline loading state (or extract to widgets/)
- The empty state ("No pulse yet") stays inline in Dashboard.tsx since it's a full-page state
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/mrm && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/dashboard/widgets/ directory exists with at least 8 .tsx files
    - src/components/dashboard/Dashboard.tsx imports from '../../lib/snooze' (not inline snooze functions)
    - src/components/dashboard/Dashboard.tsx imports from './useDashboardConfig'
    - src/components/dashboard/Dashboard.tsx contains `isVisible(` calls for widget conditional rendering
    - src/components/dashboard/DashboardSettings.tsx contains `useEscape(` for escape key handling
    - src/components/dashboard/DashboardSettings.tsx contains preset buttons for 'full' and 'focus'
    - src/components/dashboard/widgets/NeedsAttentionWidget.tsx renders both overdue and dormant contacts (contains both "overdue" and "dormant" or "gone quiet" text)
    - src/components/dashboard/widgets/QuickLinksWidget.tsx contains `getPipelines` import
    - src/components/dashboard/widgets/TodaysFocusWidget.tsx contains `/pulse/nurturing?filter=focus`
    - src/components/dashboard/widgets/ComingUpWidget.tsx contains `/pulse/nurturing?filter=dates`
    - src/components/dashboard/widgets/NeedsAttentionWidget.tsx contains `/pulse/nurturing?filter=overdue`
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Dashboard decomposed into 8 toggleable widget components. Settings panel with Full/Focus presets and widget toggle list. Overdue + dormant merged per D-04. Campaigns replaced with compact link cards per D-06. All sections render identically to current state with Full preset active. Build passes.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes with no type errors
- Dashboard renders at `http://localhost:5173/` with all sections visible (Full preset is default)
- Clicking gear icon opens settings panel
- Toggling a widget hides/shows the corresponding section
- Switching to Focus preset hides equity ring, wrapped, pod health, recent activity
- Refreshing the page preserves widget configuration
- Overdue and dormant contacts appear in a single "needs attention" section
- "See all" links appear on Today's Focus, Needs Attention, and Coming Up section headers
</verification>

<success_criteria>
Dashboard is fully decomposed into independently toggleable widgets. Settings panel supports two presets and per-widget toggles. Configuration persists in localStorage. No visual regression from current dashboard in Full preset mode. Build passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/16-dashboard-nurturing/16-01-SUMMARY.md`
</output>
