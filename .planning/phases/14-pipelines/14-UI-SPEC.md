---
phase: 14
slug: pipelines
status: draft
shadcn_initialized: true
preset: default (slate base, css variables)
created: 2026-03-29
---

# Phase 14 — UI Design Contract

> Visual and interaction contract for the Pipelines phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn (default style) | components.json |
| Preset | slate base color, cssVariables: true | components.json |
| Component library | Radix UI (via shadcn) | components.json |
| Icon library | lucide-react 1.7.0 | RESEARCH.md |
| Body font | Plus Jakarta Sans 400/600 | design-system.md / index.css |
| Heading font | Fraunces (serif) 400/600 | design-system.md / index.css |

---

## Spacing Scale

8pt grid — all values are multiples of 8 (with 4px for micro gaps). Source: design-system.md.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding, inline separators |
| sm | 8px | Card internal padding (compact rows), between quick-action buttons |
| md | 16px | Card padding, column header padding, tab bar item padding |
| lg | 24px | Column internal padding (top/bottom), panel horizontal padding |
| xl | 32px | Board horizontal padding, section breaks between tab bar and board |
| 2xl | 48px | Major layout gaps (e.g., between pipeline board and page edge) |
| 3xl | 64px | Page-level spacing (reserved, not expected in this phase) |

Exceptions:
- Stage column minimum width: 280px (fixed, not a spacing token)
- Contact avatar overlap: -8px (negative margin for stacked avatars on a card)
- Card min-height: 64px (ensures touch target compliance per Apple HIG)
- Undo toast: 8px from viewport bottom-right corner, 16px internal padding

---

## Typography

Source: design-system.md, index.css. Two fonts in use: Plus Jakarta Sans (sans) and Fraunces (serif). Two weights only: 400 (regular) and 600 (bold).

| Role | Size | Weight | Font | Line Height | Usage in Phase 14 |
|------|------|--------|------|-------------|-------------------|
| Body | 13px | 400 | Plus Jakarta Sans | 1.5 | Card opportunity name, column card list, notes text in slide-out, inline note input |
| Label | 11px | 400 | Plus Jakarta Sans | 1.4 | Contact name under avatar, priority badge label, timestamp/metadata, custom fields on card face |
| Heading | 16px | 600 | Fraunces (serif) | 1.2 | Stage column header name, pipeline tab bar selected label, slide-out panel heading |
| Display | 18px | 600 | Fraunces (serif) | 1.2 | Opportunity name in slide-out detail panel header |

Additional type notes:
- Letter-spacing on all serif headings: `-0.02em`
- Section labels (e.g., "LINKED RECORDS", "STAGE HISTORY"): 11px, weight 600, serif, uppercase, `letter-spacing: 0.04em`
- Muted/empty-state copy: 13px, weight 400, `var(--color-text-secondary)`
- Active pipeline tab text: weight 600 (Plus Jakarta Sans). Inactive tab text: weight 400.
- Priority badge text: 11px, weight 400. No weight other than 400 or 600 anywhere in this phase.

---

## Color

Source: design-system.md, index.css. All tokens already declared in `:root`.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#F5F4F0` (`--color-bg`) | Board background, page background, column body fill |
| Secondary (30%) | `rgba(255,255,255,0.92)` (`--surface-panel`) | Opportunity cards, slide-out panel, column header surface, tab bar, inline creation form |
| Accent (10%) | `#25B439` (`--color-brand`) | Active pipeline tab indicator underline/fill, nav pill active state for "Pipelines", "+" add buttons on hover |
| Destructive | `#FF3B30` (`color-overdue`) | Archive action button on card, destructive confirmation in slide-out |

Accent reserved for:
1. Active pipeline tab indicator (underline or filled background)
2. "Pipelines" nav pill active state
3. "+" button fill on hover in stage column footer and Pipelines widget on RecordPage
4. Focus ring on inline inputs (stage rename, card name creation)

Priority badge colors (semantic — not accent):
- High: `hsla(0, 70%, 50%, 0.15)` background + `hsla(0, 70%, 45%, 1)` text
- Medium: `hsla(40, 80%, 50%, 0.15)` background + `hsla(40, 80%, 40%, 1)` text
- Low: `hsla(210, 60%, 50%, 0.12)` background + `hsla(210, 60%, 45%, 1)` text

Source: RESEARCH.md code example (priority badge cycling).

Stage column header colored accent:
- Column header top border or left accent stripe matches the stage's custom color
- Header text always `var(--color-text-primary)` regardless of stage color
- Stage color swatches (for color picker): 8 preset hex values from `POD_SHIFT_COLORS` palette in SolidOrb.tsx

---

## Component Inventory

Components to build. Follow indicated pattern reference exactly.

| Component | File | Pattern Reference |
|-----------|------|-------------------|
| PipelinesPage | `src/components/pipelines/PipelinesPage.tsx` | New top-level route component |
| PipelineTabBar | `src/components/pipelines/PipelineTabBar.tsx` | Linear-style horizontal tabs; understated, fast switching |
| PipelineBoard | `src/components/pipelines/PipelineBoard.tsx` | DndContext wrapper, stage columns side-by-side |
| PipelineStageColumn | `src/components/pipelines/PipelineStageColumn.tsx` | Droppable, inline-edit header, "+ Add" footer |
| OpportunityCard | `src/components/pipelines/OpportunityCard.tsx` | Compact draggable card with quick actions |
| OpportunityDetail | `src/components/pipelines/OpportunityDetail.tsx` | `src/components/campaigns/CampaignDetail.tsx` |
| CreatePipelineModal | `src/components/pipelines/CreatePipelineModal.tsx` | Minimal inline modal, name input only |
| AddToPipelineModal | `src/components/pipelines/AddToPipelineModal.tsx` | Shared by RecordPage widget and bulk action |
| PipelinesWidget | `src/components/records/PipelinesWidget.tsx` | `src/components/records/RecordWidgets.tsx` widget container |

---

## Interaction Contracts

### PipelineTabBar

- Tabs: one per active pipeline. Fixed height 40px. Bottom-border indicator in `--color-brand` for active tab.
- Inactive tab text: `var(--color-text-secondary)`, weight 400. Active tab text: `var(--color-text-primary)`, weight 600.
- Tab overflow (more than 5 pipelines): horizontal scroll with no visible scrollbar. Fade mask at right edge.
- Hidden pipelines: muted section at end of tab bar with reduced opacity (0.45). Label "Hidden" separator in `var(--color-text-tertiary)`.
- "+" tab at the far right: icon-only (`Plus` from lucide-react), same height as tabs, `aria-label="New pipeline"`, opens `CreatePipelineModal`.
- Kebab menu on each tab (visible on hover): contains single action "Hide pipeline". Uses `MoreHorizontal` icon, 16px, `aria-label="Pipeline options"`.

### PipelineBoard (Kanban)

Primary focal point: the stage column header with its color accent stripe and Fraunces heading.

- Columns display side by side with `gap: 16px` horizontal scroll on overflow.
- Each stage column: minimum width 280px, no maximum. Background `var(--surface-panel)`, `border-radius: 12px`, `border: 1px solid var(--edge)`.
- Column header: stage name in Fraunces 16px/600, left accent stripe 3px wide in stage color. Rename on click — inline input, save on blur, escape cancels. Drag handle on header for column reorder.
- Column color picker: click color swatch in header. 8 preset hex swatches in a popover (no full color wheel). Selected swatch shows checkmark.
- Empty column state: "No opportunities" in `var(--color-text-tertiary)`, 13px, centered vertically in column body. "+ Add" button in `--color-brand` below the text.
- Column footer: persistent "+ Add opportunity" button, 13px, `--color-brand` text. Click opens inline creation form at column bottom.
- Inline card creation form: name input + optional contact link. 2 fields visible. "Add" button + escape to cancel. Form appears in-column, not modal.

### OpportunityCard

- Surface: `var(--surface-panel)`, `border-radius: 10px`, `border: 1px solid var(--edge)`, `padding: 8px 16px`.
- Drag: lift shadow on active drag — `box-shadow: 0 8px 24px rgba(0,0,0,0.12)`. Use dnd-kit `DragOverlay` for drag ghost.
- Hover state: `box-shadow: 0 2px 8px rgba(0,0,0,0.08)`. Reveal archive button (top-right, `Archive` icon 14px, `var(--color-text-tertiary)`).
- Opportunity name: 13px, weight 400, `var(--color-text-primary)`. Single line, truncate with ellipsis.
- Contact avatars: stacked row, 24px each (from `Avatar` in `src/components/ui.tsx`). Max 3 shown, "+N" overflow count. Names below avatars: 11px, weight 400, `var(--color-text-secondary)`. Click avatar navigates to `/record/:id`.
- Priority badge: right-aligned pill, 11px, weight 400. Colors per priority as declared in Color section. Click cycles `low → medium → high → low`. No confirmation.
- Custom fields marked "show on card": displayed below contact row, 11px, weight 400, `var(--color-text-secondary)`. Max 2 visible on card face.
- Inline note input: appears below card content on click (not hover). Single-line text input, 13px, weight 400. Press Enter or blur to save. Writes `pipeline_event` on save.

### OpportunityDetail (slide-out panel)

- Width: 480px. Fixed right, full viewport height. z-index: 200 (above DragOverlay).
- Entry animation: `translateX(100%) → translateX(0)`, `cubic-bezier(0.87, 0, 0.13, 1)`, 350ms. Source: `ease-panel` token.
- Background: `var(--surface-panel)` with `backdrop-filter: blur(32px)`.
- Escape closes (via `useEscape` from `src/lib/escapeStack.ts`). Dim overlay behind panel: `rgba(0,0,0,0.08)`.
- Header: opportunity name in Fraunces 18px/600. Pipeline name + current stage in 13px/400, `var(--color-text-secondary)`.
- Sections (in order): Linked Records, Priority + Status, Notes, Stage History, Custom Fields.
- Section labels: 11px, weight 600, Fraunces, uppercase, `letter-spacing: 0.04em`, `var(--color-text-tertiary)`.
- All editable fields: inline-edit-on-click pattern (same as PodDetailPage). Save on blur.
- Stage history entries: compact dot-line format (same as system events in timeline). `pipeline_event` renderer already built in Phase 13.
- Linked records list: each row shows Avatar + name + role. Click navigates to `/record/:id`.

### UndoToast

- Position: fixed, bottom-right, `bottom: 24px`, `right: 24px`.
- Surface: `rgba(0,0,0,0.82)` (dark), `color: #ffffff`, `border-radius: 10px`, `padding: 8px 16px`.
- Text: "{Opportunity name} moved to {stage name}" in 13px/400. "Undo" button in 13px/600, `--color-brand` text.
- Duration: 5 seconds auto-dismiss. `setTimeout` with cleanup on undo click.
- One toast at a time — new action replaces existing toast and resets timer.
- Archive toast: same treatment — "{Opportunity name} archived. Undo".

### PipelinesWidget (RecordPage)

- Widget container: matches existing widget pattern in `RecordWidgets.tsx`.
- Header: "Pipelines" label in 11px/600/Fraunces uppercase. "+" icon button right-aligned opens `AddToPipelineModal`.
- Empty: "No pipeline associations" in `var(--color-text-tertiary)`, 13px, centered.
- Opportunity row: opportunity name (13px/400) + pipeline name + stage name (11px/400, `var(--color-text-secondary)`). Custom fields marked "show on card" appear as smaller text below.
- Row click: navigate to `/pipelines?pipeline={id}&opportunity={id}` (D-17). Opens board with that pipeline tab active and OpportunityDetail open.

### AddToPipelineModal

- Triggered from: (1) "+" in PipelinesWidget, (2) "Add to pipeline" bulk action in RecordsList.
- Layout: small centered modal or bottom sheet (not slide-out). Width 400px, `border-radius: 16px`.
- Fields: Pipeline dropdown (select), Stage dropdown (select — populated from selected pipeline). "Add Opportunity" button in `--color-brand`.
- Bulk variant: shows count "Adding to pipeline for {N} contacts" before the dropdowns.
- Escape closes. Click outside closes.

### CreatePipelineModal

- Small centered modal, width 360px.
- Single field: pipeline name text input, autofocused.
- "Create Pipeline" button + Escape to cancel.
- On submit: calls `createPipeline()`, adds new tab to PipelineTabBar, sets new pipeline as active.

---

## Motion

Source: design-system.md motion tokens.

| Interaction | Curve | Duration |
|-------------|-------|----------|
| Slide-out panel enter | `cubic-bezier(0.87, 0, 0.13, 1)` | 350ms |
| Card drag lift | `ease-out` | 120ms |
| Card drop settle | `cubic-bezier(0.215, 0.61, 0.355, 1)` | 220ms |
| Tab switch | `ease-out` | 150ms |
| Undo toast appear | `ease-out` | 200ms |
| Column reorder | `cubic-bezier(0.215, 0.61, 0.355, 1)` | 220ms |
| Priority badge cycle | instant (no animation) | — |

---

## Copywriting Contract

| Element | Copy | Context |
|---------|------|---------|
| Primary CTA (create pipeline) | "Create Pipeline" | Submit button in CreatePipelineModal |
| Primary CTA (create opportunity) | "Add Opportunity" | Footer button in stage column; submit button in AddToPipelineModal |
| Primary CTA (add to pipeline from record) | "Add to Pipeline" | "+" button in PipelinesWidget header |
| Primary CTA (bulk action) | "Add to Pipeline" | Bulk action bar in RecordsList |
| Empty board heading | "No opportunities in this stage" | Inside an empty stage column |
| Empty board sub-copy | "Drag a card here or add one below." | Below empty state heading in column |
| Empty widget heading | "Not in any pipelines" | PipelinesWidget on RecordPage with no associations |
| Empty widget sub-copy | "Add this contact to a pipeline to track progress." | Below empty widget heading |
| Error: stage move failed | "Couldn't move card. Changes reverted." | Replaces undo toast if write fails (auto-dismiss 4s) |
| Error: create opportunity failed | "Couldn't create opportunity. Try again." | Inline below form, no modal |
| Error: pipeline load failed | "Couldn't load pipelines. Refresh to try again." | Full-board error state, centered |
| Archive confirmation | None — undo toast only | Archive is low-stakes; undo toast is sufficient (no modal confirm) |
| Hide pipeline confirmation | "Hide pipeline? You can unhide it from the tab bar anytime." | Inline confirmation below kebab menu item |
| Delete opportunity (if needed) | Not in this phase — archive is the only removal action | — |
| Undo toast (stage move) | "{Opportunity name} moved to {Stage name}. Undo" | Bottom-right toast |
| Undo toast (archive) | "{Opportunity name} archived. Undo" | Bottom-right toast |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | None — no pre-built blocks used; all components hand-built following existing patterns | not required |
| Third-party | None declared | not applicable |

No third-party registries. dnd-kit is an npm package, not a shadcn registry block. Registry vetting gate: not triggered.

---

## Accessibility Notes

- All interactive card elements (priority badge click, archive button, avatar click): minimum 24px touch target. Archive button on hover must have `aria-label="Archive opportunity"`.
- Drag-and-drop: dnd-kit provides keyboard accessibility by default. Do not suppress keyboard handlers.
- Stage column header rename input: `aria-label="Rename stage"` when in edit mode.
- Undo toast: `role="status"` and `aria-live="polite"` for screen reader announcement.
- Empty state text in columns: `var(--color-text-tertiary)` at 13px — meets WCAG AA for normal text (3.2:1 is borderline; use 13px minimum, not 11px for empty state body copy).
- OpportunityDetail close button: `aria-label="Close opportunity detail"`.
- "+" tab in PipelineTabBar: `aria-label="New pipeline"`.
- Kebab menu button on each pipeline tab: `aria-label="Pipeline options"`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
