---
phase: 11-relationship-records
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/records/CreateRecordModal.tsx
  - src/components/records/AssociatedPeopleWidget.tsx
  - src/components/records/RecordHeader.tsx
  - src/lib/airtable.ts
  - src/lib/sampleData.ts
autonomous: true
requirements: [REC-01, REC-02, REC-04, CRE-01, CRE-04]

must_haves:
  truths:
    - "User can create a Contact record with first name, last name, email, company, and pod assignment"
    - "User can create a Company record with name, industry, domain, and pod assignment"
    - "Company creation warns on duplicate names (case-insensitive)"
    - "Contact header shows company as a typeahead-linkable field that navigates to the company record"
    - "Company records show an Associated People widget listing linked contacts"
  artifacts:
    - path: "src/components/records/CreateRecordModal.tsx"
      provides: "Type-first creation modal with Contact + Company forms"
      min_lines: 150
    - path: "src/components/records/AssociatedPeopleWidget.tsx"
      provides: "Company-only widget listing linked contacts with add search"
      min_lines: 50
  key_links:
    - from: "src/components/records/CreateRecordModal.tsx"
      to: "src/lib/airtable.ts"
      via: "createContact() with type and company_record_id"
      pattern: "createContact"
    - from: "src/components/records/RecordHeader.tsx"
      to: "/record/:id"
      via: "company name click navigates to company record"
      pattern: "navigate.*record/"
    - from: "src/components/records/AssociatedPeopleWidget.tsx"
      to: "src/lib/airtable.ts"
      via: "getContacts filtered by company_record_id"
      pattern: "company_record_id"
---

<objective>
Build the CreateRecordModal (type-first flow with Contact and Company forms) and the company-contact linking UI (typeahead on Contact header, Associated People widget on Company records).

Purpose: Users need to create new relationship records and link contacts to companies. This is the write path — Plan 01 handles the read/display path.

Output: Working creation modal, company typeahead, Associated People widget.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-relationship-records/11-CONTEXT.md
@.planning/phases/11-relationship-records/11-RESEARCH.md
@.planning/phases/11-relationship-records/11-UI-SPEC.md

<interfaces>
<!-- From Plan 01 — RecordHeader will be modified to add company typeahead -->

From src/lib/airtable.ts:
```typescript
export async function createContact(data: Omit<Contact, 'id' | 'created_at'>): Promise<Contact>
export async function updateContact(id: string, data: Partial<Omit<Contact, 'id' | 'created_at'>>): Promise<Contact>
export async function getContacts(categoryId?: string): Promise<Contact[]>
export async function getContactsByType(type: RelationshipType): Promise<Contact[]>
export async function getPods(): Promise<Pod[]>
```

From src/lib/types.ts:
```typescript
export interface Contact {
  type: RelationshipType        // 'Contact' | 'Company'
  status: RelationshipStatus    // 'Active' | 'Pending' | 'Archived'
  company_record_id: string | null
  list_ids: string[]            // pod IDs
  first_name: string | null
  last_name: string | null
  name: string                  // computed as "first last" or company name
  // ... all other fields
}
```

From src/components/search/SearchPalette.tsx:
- Debounced typeahead pattern: setTimeout 150ms in useEffect, filter contacts cache, render dropdown
- Keyboard navigation: up/down/enter/escape

From src/components/contacts/AddContactModal.tsx:
- Existing creation modal with structured + brain dump modes
- Will be superseded by CreateRecordModal

From src/components/map/SolidOrb.tsx:
```typescript
export const POD_SHIFT_COLORS: Record<string, string>  // pod hex -> shift color
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CreateRecordModal — type-first flow with Contact and Company forms</name>
  <files>src/components/records/CreateRecordModal.tsx, src/lib/airtable.ts</files>
  <read_first>
    - src/components/contacts/AddContactModal.tsx (existing creation modal — brain dump mode pattern, form structure)
    - src/lib/airtable.ts (createContact function, getContactsByType, getPods)
    - src/lib/sampleData.ts (DEMO_PODS for pod multi-select in demo mode)
    - src/lib/escapeStack.ts (useEscape hook for modal dismiss)
    - src/components/map/SolidOrb.tsx (POD_SHIFT_COLORS for pod chip colors)
    - .planning/phases/11-relationship-records/11-UI-SPEC.md (CreateRecordModal layout spec, interaction contract)
  </read_first>
  <action>
**Build CreateRecordModal.tsx** per D-22, D-23, D-24, D-25, CRE-01, CRE-04:

**Step 1 — Type picker:**
- Modal overlay: fixed, full viewport, `rgba(0,0,0,0.3)` backdrop, centered content
- Modal card: max-width 480px, background `rgba(255,255,255,0.92)`, border-radius 16px, box-shadow `0 8px 32px rgba(0,0,0,0.16)`, padding `24px`
- Heading: "New Relationship" in Fraunces 16px weight 700
- Two type cards side by side (50/50):
  - "Contact" card: label "Contact" (13px sans weight 700), subtitle "Person" (11px sans weight 400, `rgba(0,0,0,0.45)`)
  - "Company" card: label "Company", subtitle "Organization"
  - Card style: border `1px solid rgba(0,0,0,0.07)`, border-radius 12px, padding `16px`, cursor pointer, min-height 80px
  - Hover: border color `#25B439`
  - Selected: background `#25B439`, text white `rgba(255,255,255,0.92)`, border color `#25B439`
- Clicking a type card advances to Step 2 (set state `step: 'form'`, `recordType: 'Contact' | 'Company'`)
- Escape dismisses modal (use `useEscape` from escapeStack.ts)

**Step 2 — Contact form** (when `recordType === 'Contact'`):
- Fields:
  - First Name* (text input, required)
  - Last Name* (text input, required) — same row as First Name
  - Email (text input, optional)
  - Company (typeahead search dropdown — searches Company records via `getContactsByType('Company')`, 150ms debounce, max 5 results. No match + input > 2 chars: show `+ Create "[input]" as company`. Selecting "+ Create" creates a Company record inline via `createContact({ name: input, type: 'Company', ... })` then uses its ID as `company_record_id`)
  - Pod* (multi-select with colored chips, required) — load pods via `getPods()`, render as selectable list, each selected pod shows as chip with `POD_SHIFT_COLORS[pod.color]` bg + white text
- Brain dump toggle: "Switch to brain dump" link below form. When active, replaces all fields with single `<textarea>` placeholder "Dump everything you know...". Saves with `status: 'Pending'` and `needs_review: true`.
- Required fields marked with `*` in `#25B439` after label text
- Validation: "Create Contact" button disabled until first_name AND last_name AND at least one pod selected. Button tooltip when disabled: "Fill required fields"
- CTA: "Create Contact" — full width, `#25B439` bg, white text, border-radius 8px, padding `12px`, 13px sans weight 700

**Step 2 — Company form** (when `recordType === 'Company'`):
- Fields:
  - Company Name* (text input, required) — on blur with > 2 chars, run duplicate detection: `getContactsByType('Company').filter(c => c.name.toLowerCase().trim() === input.toLowerCase().trim())`. If match found, show inline warning: "A company named '[name]' already exists. Link to it instead?" with a "Link" button that cancels creation and returns the existing record.
  - Industry (text input, optional)
  - Domain (text input, optional, placeholder "example.com")
  - Pod* (same multi-select as Contact form)
- CTA: "Create Company" — same style as Contact CTA

**On submit:**
- Build Contact object: `name` = `${first_name} ${last_name}` for Contact, or company name for Company. Set `type`, `status: 'Active'`, `list_ids` from selected pods, `company_record_id` from typeahead selection (Contact only).
- Call `createContact(data)` from airtable.ts
- On success: call `onCreated(newRecord)` prop, close modal
- On error: show inline error "Couldn't create the record. Check your connection and try again." below the CTA

**Props:** `isOpen: boolean, onClose: () => void, onCreated: (contact: Contact) => void`

**2. Wire the modal trigger** — The modal will be triggered from a "+" button in the nav or a "New Record" button. For now, export the component. The parent (App.tsx or RecordPage) will integrate it. Add a floating "+" FAB to App.tsx:
- Position: fixed, bottom 100px (above nav pill), right 24px
- Size: 48px circle, `#25B439` bg, white "+" text (20px), box-shadow
- onClick: opens `<CreateRecordModal />`
- After successful creation: `navigate('/record/${newRecord.id}')`
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/mrm && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/records/CreateRecordModal.tsx exists and contains `step` state management (type picker -> form)
    - CreateRecordModal contains `recordType === 'Contact'` and `recordType === 'Company'` conditional rendering
    - CreateRecordModal contains `createContact` import and call
    - CreateRecordModal contains company duplicate detection: `getContactsByType('Company')` and `.toLowerCase().trim()`
    - CreateRecordModal contains pod multi-select with `getPods()` and `POD_SHIFT_COLORS`
    - CreateRecordModal contains brain dump toggle with `needs_review: true`
    - CreateRecordModal CTA button is disabled when required fields are empty: `disabled={!firstName || !lastName || !selectedPods.length}` (or equivalent)
    - App.tsx contains CreateRecordModal import and the FAB button with `#25B439` background
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>CreateRecordModal renders a type-first flow — pick Contact or Company, fill type-appropriate form with required field validation, pod assignment enforced, company typeahead with inline creation, brain dump mode for Contacts, duplicate detection for Companies. FAB button in App.tsx triggers creation flow.</done>
</task>

<task type="auto">
  <name>Task 2: Company-contact linking — typeahead on Contact header + AssociatedPeopleWidget</name>
  <files>src/components/records/RecordHeader.tsx, src/components/records/AssociatedPeopleWidget.tsx, src/components/records/RecordWidgets.tsx</files>
  <read_first>
    - src/components/records/RecordHeader.tsx (from Plan 01 Task 2 — need to add company typeahead to Contact header)
    - src/components/records/RecordWidgets.tsx (from Plan 01 Task 2 — need to add AssociatedPeopleWidget slot)
    - src/lib/airtable.ts (getContacts, getContactsByType, updateContact)
    - src/lib/escapeStack.ts (useEscape for dropdown dismiss)
    - .planning/phases/11-relationship-records/11-UI-SPEC.md (company typeahead interaction contract, AssociatedPeopleWidget empty state)
  </read_first>
  <action>
**1. Add company typeahead to RecordHeader.tsx** per D-14, D-15, D-17:

Only render for Contact records (`contact.type === 'Contact'`). Below the name row, show the company association:

- If `contact.company_record_id` is set: display company name as a clickable link (13px sans, `#25B439`, cursor pointer). Load company name by finding the record in contacts cache: `getContacts().then(all => all.find(c => c.id === contact.company_record_id))`. Click navigates to `/record/${contact.company_record_id}`.
- "Change" text button next to company name (11px sans, `rgba(0,0,0,0.28)`) — clicking opens the typeahead.
- If no company linked: show "Add company" text button (11px sans, `rgba(0,0,0,0.28)`) — clicking opens typeahead.

**Typeahead implementation** (same pattern as SearchPalette):
- Input: 13px sans, no border, just bottom border `1px solid #25B439` when focused
- Debounce: 150ms via `setTimeout` in `useEffect` watching query state
- Results source: `getContactsByType('Company')` → filter by `name.toLowerCase().includes(query.toLowerCase())`
- Dropdown: absolute positioned below input, max 5 results, background `rgba(255,255,255,0.96)`, border `1px solid rgba(0,0,0,0.07)`, border-radius 8px, box-shadow `0 4px 16px rgba(0,0,0,0.1)`, each row 44px min-height
- Matching text bolded in results (wrap match substring in `<strong>`)
- No results + query > 2 chars: show `+ Create "[query]" as company` row — clicking creates company via `createContact({ name: query, type: 'Company', status: 'Active', list_ids: [], ... })` then updates the current contact's `company_record_id` to the new company's ID
- On selecting existing company: call `onUpdate({ company_record_id: selectedCompany.id, company: selectedCompany.name })` — updates both the linked field and the backward-compat text field
- Escape closes dropdown (use `useEscape` from escapeStack.ts)
- Blur closes dropdown after 150ms delay (to allow click on dropdown items)

**2. Build AssociatedPeopleWidget.tsx** per D-14, D-16:

Only rendered on Company records. Shows contacts whose `company_record_id` matches this company's ID.

- Widget card style: same as DetailsWidget (background `rgba(255,255,255,0.92)`, border, border-radius 12px, padding `16px 20px`, marginBottom 12px)
- Card title: "People" in Fraunces 16px weight 700
- Load associated contacts: from the contacts cache, filter `getContacts().then(all => all.filter(c => c.company_record_id === contact.id))`
- Each person row: Avatar + name (13px sans) + role (11px sans, `rgba(0,0,0,0.45)`). Click navigates to `/record/${person.id}`. Min-height 44px.
- Empty state: "No linked contacts" heading, "Search for existing contacts to link." body (per UI-SPEC states)
- "+ Add person" button at bottom: 11px sans weight 700, color `#25B439`
- Clicking "+ Add person" opens a typeahead inline (same debounced search pattern, searches Contact-type records). On select: updates the selected contact's `company_record_id` to this company's ID via `updateContact(selectedContact.id, { company_record_id: contact.id, company: contact.name })`
- After linking, the new person appears in the list immediately (refetch or optimistic update)

**3. Wire AssociatedPeopleWidget into RecordWidgets.tsx:**

In RecordWidgets, after HealthWidget and the PodFieldsWidget placeholder, add:
```tsx
{contact.type === 'Company' && (
  <AssociatedPeopleWidget contact={contact} />
)}
```

Import AssociatedPeopleWidget at top of RecordWidgets.tsx.
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/mrm && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - RecordHeader.tsx contains `contact.type === 'Contact'` guard for company typeahead section
    - RecordHeader.tsx contains `getContactsByType('Company')` for typeahead results
    - RecordHeader.tsx contains `company_record_id` in an `onUpdate` call when linking company
    - RecordHeader.tsx contains `navigate` to `/record/` for clicking company name link
    - AssociatedPeopleWidget.tsx exists and contains `company_record_id === contact.id` filter
    - AssociatedPeopleWidget.tsx contains `updateContact` call for linking people
    - RecordWidgets.tsx contains `contact.type === 'Company'` and `AssociatedPeopleWidget`
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Contact records show company as a clickable link with typeahead to change/set. Company records show Associated People widget with linked contacts and add-person search. Linking updates company_record_id in Airtable — relationship visible on both records per D-17.</done>
</task>

</tasks>

<verification>
1. `pnpm build` passes
2. Click "+" FAB → CreateRecordModal opens → pick Contact → fill form → create → navigates to new record
3. Pick Company → fill form → duplicate detection triggers on matching name → can still create or link
4. On Contact record: company field shows as link, click navigates to company record. Change company via typeahead.
5. On Company record: Associated People widget lists linked contacts. "+ Add person" links a contact.
</verification>

<success_criteria>
- CreateRecordModal creates Contact and Company records with required field validation
- Pod assignment is mandatory — cannot create without at least one pod
- Company duplicate detection warns on case-insensitive name match
- Company typeahead on Contact header links records via company_record_id
- Associated People widget on Company records lists and adds linked contacts
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-relationship-records/11-02-SUMMARY.md`
</output>
