---
phase: 12-pods-overhaul-categorization
plan: 03
type: execute
wave: 3
depends_on: ["12-01"]
files_modified:
  - src/components/pods/PodDetailPage.tsx
  - src/components/pods/PodCreateModal.tsx
  - src/App.tsx
autonomous: true
requirements: [POD-01, POD-03, POD-10]

must_haves:
  truths:
    - "User can navigate to /pod/:id and see pod settings, required fields, sub-pods, and members"
    - "User can create a new pod from a modal (triggered from orb map + button or pod management)"
    - "Pod detail page shows member list with equity scores"
    - "Pod detail page shows sub-pods (categories) list"
    - "Pod detail page shows pod-scoped field configs (required questions)"
  artifacts:
    - path: "src/components/pods/PodDetailPage.tsx"
      provides: "Full pod detail page at /pod/:id"
      min_lines: 100
    - path: "src/components/pods/PodCreateModal.tsx"
      provides: "Pod creation modal with name, color, description, cadence, capacity"
    - path: "src/App.tsx"
      provides: "Route for /pod/:id"
      contains: "pod/:id"
  key_links:
    - from: "App.tsx"
      to: "PodDetailPage.tsx"
      via: "Route path='pod/:id'"
      pattern: "Route.*pod/:id.*PodDetailPage"
    - from: "PodDetailPage.tsx"
      to: "airtable.ts"
      via: "getPods, getContacts, getFieldConfigs, getCategories"
      pattern: "getPods|getContacts|getFieldConfigs|getCategories"
    - from: "PodCreateModal.tsx"
      to: "airtable.ts"
      via: "createPod function"
      pattern: "createPod"
---

<objective>
Build the pod detail page at `/pod/:id` and pod creation modal. The detail page consolidates pod settings, required fields, sub-pods, and members in one place.

Purpose: Pods become first-class navigable entities with their own detail page, replacing the in-map category drill behavior.
Output: PodDetailPage component, PodCreateModal component, `/pod/:id` route in App.tsx.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-pods-overhaul-categorization/12-CONTEXT.md
@.planning/phases/12-pods-overhaul-categorization/12-RESEARCH.md
@.planning/phases/12-pods-overhaul-categorization/12-01-SUMMARY.md

@src/App.tsx
@src/lib/types.ts
@src/lib/airtable.ts
@src/components/records/RecordPage.tsx
@src/components/contacts/CategoryTable.tsx
@docs/design-system.md

<interfaces>
<!-- Pod interface (after 12-01) -->
export interface Pod {
  id: string
  name: string
  color: HexColor | null
  owner: Owner | null
  is_priority: boolean
  cadence: Cadence | null
  description: string | null
  capacity: number | null
  created_at: string
}

<!-- Category interface -->
export interface Category {
  id: string
  list_id: string
  name: string
  color: HexColor | null
  created_at: string
}

<!-- FieldConfig interface -->
export interface FieldConfig {
  id: string
  name: string
  field_type: 'text' | 'multiline' | 'number' | 'select' | 'date' | 'checkbox'
  scope_type: RelationshipType | null
  scope_pod_id: string | null
  required: boolean
  options: string[] | null
  table_id: string | null
}

<!-- Available CRUD from airtable.ts -->
export async function getPods(): Promise<Pod[]>
export async function getContacts(): Promise<Contact[]>
export async function getActiveContacts(): Promise<Contact[]>
export async function getCategories(listId?: string): Promise<Category[]>
export async function getFieldConfigs(): Promise<FieldConfig[]>
export async function getAllInteractions(): Promise<Interaction[]>
export async function createPod(data: ...): Promise<Pod>
export async function updatePod(id: string, data: ...): Promise<Pod>

<!-- Equity scoring -->
export function podEquityScore(podId: string, contacts: Contact[], interactionsByContact: Map<string, Interaction[]>, cadence?: Cadence): number
export function contactEquityScore(interactions: Interaction[], cadenceDays: number): number
export function scoreLabel(score: number): ScoreLabel

<!-- POD_SHIFT_COLORS from SolidOrb -->
export const POD_SHIFT_COLORS: Record<string, string>

<!-- Current App.tsx routes -->
<Route index element={<Dashboard />} />
<Route path="map" element={<OrbMap />} />
<Route path="category/:id" element={<CategoryTable />} />
<Route path="record/:id" element={<RecordPage />} />
<Route path="import" element={<ImportPanel />} />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PodDetailPage at /pod/:id</name>
  <files>src/components/pods/PodDetailPage.tsx, src/App.tsx</files>
  <read_first>src/App.tsx, src/components/records/RecordPage.tsx (for two-column layout pattern), src/components/contacts/CategoryTable.tsx (for member list pattern with equity scores), src/lib/airtable.ts (getPods, getContacts, getCategories, getFieldConfigs, getAllInteractions, updatePod), src/lib/equity.ts (podEquityScore, contactEquityScore, scoreLabel, indexByContact, contactCadenceDays), docs/design-system.md</read_first>
  <action>
1. Create `src/components/pods/PodDetailPage.tsx`:
   - Use `useParams<{ id: string }>()` to get pod ID from route
   - Load data on mount: `getPods()`, `getContacts()`, `getCategories(podId)`, `getFieldConfigs()`, `getAllInteractions()`
   - Find pod by ID from pods array. If not found, show "Pod not found" with back link.
   - Compute members: `contacts.filter(c => c.status === 'Active' && c.list_ids.includes(podId))`
   - Compute equity scores: use `indexByContact(interactions)` then per-member `contactEquityScore()`
   - Compute pod-scoped fields: `fieldConfigs.filter(fc => fc.scope_pod_id === podId)`

   **Layout per D-12 — full page, two sections:**

   **Top section — Pod header + settings (editable):**
   - Pod color dot (32px circle with pod.color background) + pod name (Fraunces serif, 28px, weight 700)
   - Description: editable textarea, auto-saves on blur via `updatePod(podId, { description: newValue })`
   - Settings row: Cadence (dropdown: weekly/biweekly/monthly/quarterly), Capacity (number input, empty = unlimited), Owner (dropdown), Priority (checkbox)
   - All settings save on change via `updatePod(podId, { field: value })`
   - If capacity is set, show member count: "{members.length}/{capacity}" with color coding (green if under, amber if at/over)

   **Below header — three sections in vertical scroll:**

   a) **Required Fields section:**
   - Header: "Required Questions" with count badge
   - List each FieldConfig where `scope_pod_id === podId`
   - Each shows: field name, type badge (text/select/date/etc), required indicator (red dot if required)
   - "Add Field" link at bottom — navigates to `/record/:anyMemberId` where PodFieldsWidget exists (or just show a note "Manage fields from any member's record page"). Field management was built in Phase 11 via PodFieldsWidget — don't rebuild it here.

   b) **Sub-pods section (D-15 — categories are sub-pods):**
   - Header: "Sub-pods" with count
   - Grid of sub-pod chips showing category name + color dot
   - Each chip links to `/category/:categoryId` (existing CategoryTable route)
   - "Add Sub-pod" button — creates category via existing `createCategory()` function (prompt for name + color)

   c) **Members section:**
   - Header: "Members" with count (and capacity fraction if set)
   - Sort members by equity score descending
   - Each member row: Avatar (from ui.tsx), name, company/role, equity score (0-100) with score label color, primary pod badge if this pod is their primary_list_id
   - Click member row → `navigate('/record/${member.id}')`
   - Empty state: "No members yet" with "Add from Categorization Queue" suggestion

   **Back navigation:** Arrow button in top-left, `navigate(-1)` or `navigate('/map')`

   **Pod color system:** Use `POD_SHIFT_COLORS` for any gradient effects. Import from `../map/SolidOrb`.

2. Add route to `src/App.tsx`:
   - Import PodDetailPage: `import { PodDetailPage } from './components/pods/PodDetailPage'`
   - Add route inside the `<Route element={<AppShell />}>` block: `<Route path="pod/:id" element={<PodDetailPage />} />`
   - Place it after the `record/:id` route
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/mrm && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PodDetailPage.tsx exists and is >= 100 lines
    - PodDetailPage.tsx contains `useParams` with `id` parameter
    - PodDetailPage.tsx contains `getPods()` and `getContacts()` and `getCategories` and `getFieldConfigs` calls
    - PodDetailPage.tsx contains `updatePod` call for inline editing
    - PodDetailPage.tsx contains `contactEquityScore` or `podEquityScore` for member scores
    - PodDetailPage.tsx contains `fc.scope_pod_id === podId` or equivalent for filtering field configs
    - PodDetailPage.tsx contains `navigate('/record/` for member click navigation
    - App.tsx contains `path="pod/:id"` route
    - App.tsx contains `PodDetailPage` import
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Pod detail page renders at /pod/:id showing settings (editable), required fields list, sub-pods list, and member list with equity scores. Route registered in App.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: PodCreateModal for new pod creation</name>
  <files>src/components/pods/PodCreateModal.tsx</files>
  <read_first>src/components/records/CreateRecordModal.tsx (for modal pattern), src/lib/airtable.ts (createPod), src/components/map/SolidOrb.tsx (POD_SHIFT_COLORS for color options), docs/design-system.md</read_first>
  <action>
1. Create `src/components/pods/PodCreateModal.tsx`:
   - Props: `isOpen: boolean`, `onClose: () => void`, `onCreated: (pod: Pod) => void`
   - If `!isOpen` return null
   - Overlay: `position: fixed, inset: 0, z-index: 200, background: rgba(0,0,0,0.5)`
   - Modal card: centered, max-width 480px, `var(--surface-panel)` background, `var(--panel-radius)` border-radius

   **Form fields per D-13/D-14:**
   - Name (required): text input, placeholder "e.g., Maps, LPs, Talent"
   - Color: row of color swatches. Use the keys from `POD_SHIFT_COLORS` as available colors (these are the established pod colors in the app). Click to select. Selected shows checkmark overlay.
   - Description (optional): textarea, placeholder "What is this pod for?"
   - Cadence: dropdown select — weekly, biweekly, monthly (default), quarterly
   - Capacity (optional): number input, placeholder "Leave empty for unlimited"
   - Owner: dropdown — moj_mahdara, kinship_ventures
   - Priority: checkbox — "Mark as priority pod"

   **Validation:** Name is required. All other fields optional. Save button disabled if name is empty or saving.

   **Save:**
   ```
   const pod = await createPod({
     name, color: selectedColor, owner, is_priority: isPriority,
     cadence, description: description || null, capacity: capacity || null,
   })
   onCreated(pod)
   ```

   **Close:** `useEscape(onClose)` for escape key. X button in header. Click outside modal closes.

   Note: This modal will be consumed by OrbMap's '+' orb (wired in plan 12-04) and potentially by PodDetailPage for a "create sub-pod" flow in the future. For now it's a standalone component.
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/mrm && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PodCreateModal.tsx exists
    - PodCreateModal.tsx contains `createPod` import and call
    - PodCreateModal.tsx contains `isOpen` prop check (returns null if false)
    - PodCreateModal.tsx contains `useEscape` import
    - PodCreateModal.tsx contains name validation (save disabled when empty)
    - PodCreateModal.tsx contains color swatches from POD_SHIFT_COLORS
    - PodCreateModal.tsx contains capacity number input
    - PodCreateModal.tsx contains cadence dropdown with weekly/biweekly/monthly/quarterly options
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>PodCreateModal renders a creation form with name, color picker, description, cadence, capacity, owner, priority. Validates name required. Calls createPod on save. Ready to wire into OrbMap and other surfaces.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes
- `/pod/:id` route exists in App.tsx
- PodDetailPage loads and displays pod data with members, sub-pods, and field configs
- PodCreateModal validates and creates pods via createPod
</verification>

<success_criteria>
Pod detail page live at /pod/:id showing editable settings, required fields, sub-pods, and scored member list. Pod creation modal ready for wiring. Both components follow established design system tokens and patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/12-pods-overhaul-categorization/12-03-SUMMARY.md`
</output>
