---
phase: 12-pods-overhaul-categorization
plan: 04
type: execute
wave: 4
depends_on: ["12-02", "12-03"]
files_modified:
  - src/components/map/OrbMap.tsx
  - src/components/map/ListNode.tsx
  - src/components/map/SolidOrb.tsx
  - src/components/map/CreateCategoryNode.tsx
autonomous: false
requirements: [POD-10, CAT-01]

must_haves:
  truths:
    - "Clicking a pod orb on the map navigates to /pod/:id (not category drill)"
    - "Category-drill view state is fully removed from OrbMap"
    - "Pod orbs show capacity indicator for capacity-limited pods"
    - "The '+' orb on the home view opens PodCreateModal"
    - "Orb map home view still renders all pod orbs with health rings"
  artifacts:
    - path: "src/components/map/OrbMap.tsx"
      provides: "Simplified orb map without category-drill state"
    - path: "src/components/map/ListNode.tsx"
      provides: "Pod orb that navigates to /pod/:id on click"
  key_links:
    - from: "ListNode.tsx"
      to: "react-router"
      via: "useNavigate → navigate('/pod/${id}')"
      pattern: "navigate.*pod/"
    - from: "OrbMap.tsx"
      to: "PodCreateModal.tsx"
      via: "CreateCategoryNode click opens PodCreateModal"
      pattern: "PodCreateModal|showCreatePod"
---

<objective>
Rewire the orb map: pod orb clicks navigate to /pod/:id instead of category-drill. Remove all category-drill state from OrbMap. Add capacity indicator to pod orbs. Wire '+' orb to PodCreateModal.

Purpose: Completes the pod navigation overhaul. The orb map becomes a pod browser only — all sub-pod and member management moves to the pod detail page.
Output: Simplified OrbMap, navigation-wired ListNode, capacity badge on orbs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-pods-overhaul-categorization/12-CONTEXT.md
@.planning/phases/12-pods-overhaul-categorization/12-RESEARCH.md
@.planning/phases/12-pods-overhaul-categorization/12-01-SUMMARY.md
@.planning/phases/12-pods-overhaul-categorization/12-02-SUMMARY.md
@.planning/phases/12-pods-overhaul-categorization/12-03-SUMMARY.md

@src/components/map/OrbMap.tsx
@src/components/map/ListNode.tsx
@src/components/map/SolidOrb.tsx
@src/components/map/CategoryNode.tsx
@src/components/map/CreateCategoryNode.tsx
@src/components/map/MojNode.tsx

<interfaces>
<!-- ListNode current pattern — calls onListClick prop -->
<!-- OrbMap passes handleListClick which triggers category drill -->
<!-- Category drill state: viewState, selectedListId, buildCategoryNodes, buildCategoryEdges -->

<!-- Pod (after 12-01) -->
export interface Pod {
  id: string; name: string; color: HexColor | null; capacity: number | null; ...
}

<!-- PodCreateModal (from 12-03) -->
export function PodCreateModal(props: { isOpen: boolean, onClose: () => void, onCreated: (pod: Pod) => void }): JSX.Element

<!-- SolidOrb props -->
export function SolidOrb(props: { size: number, color: string, label: string, healthPercent?: number, ... }): JSX.Element
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire ListNode navigation + remove category-drill from OrbMap</name>
  <files>src/components/map/ListNode.tsx, src/components/map/OrbMap.tsx</files>
  <read_first>src/components/map/ListNode.tsx, src/components/map/OrbMap.tsx, src/components/map/CategoryNode.tsx, src/components/map/CreateCategoryNode.tsx</read_first>
  <action>
1. Update `src/components/map/ListNode.tsx` per D-18:
   - Add `import { useNavigate } from 'react-router'`
   - Inside the component: `const navigate = useNavigate()`
   - Replace the current click handler (which calls `data.onListClick?.(data.id)` or similar prop callback) with: `navigate('/pod/' + data.id)`
   - Remove the `onListClick` prop/data field if it was part of the node data interface
   - Keep all other ListNode behavior: SolidOrb rendering, health ring, pod color, label

2. Add capacity indicator to ListNode per D-17:
   - If `data.capacity` exists (capacity-limited pod), show a small fraction label below the orb label: "{memberCount}/{capacity}"
   - `data.memberCount` and `data.capacity` passed as node data from OrbMap's `buildHomeNodes`
   - Style: fontSize 10, text-secondary color, centered below pod name
   - Only show for pods where `capacity !== null`

3. Clean up `src/components/map/OrbMap.tsx` per D-18 and Research pitfall 2:
   - Remove ALL category-drill state and logic:
     * Remove `viewState` state (or whatever state tracks 'lists' vs 'categories' view)
     * Remove `selectedListId` state
     * Remove `buildCategoryNodes()` function
     * Remove `buildCategoryEdges()` function
     * Remove `handleListClick()` function (the one that triggers category drill)
     * Remove any `useEffect` or logic that transitions between home and category views
     * Remove the back button / navigation that returns from category view to home view
   - Keep: `buildHomeNodes()`, `buildHomeEdges()`, MojNode, all home-view layout logic
   - In `buildHomeNodes()`: add `capacity` and `memberCount` to each list node's data object:
     ```
     // For each pod node:
     data: {
       ...existingData,
       capacity: pod.capacity,
       memberCount: activeContacts.filter(c => c.list_ids.includes(pod.id)).length,
     }
     ```
     This requires passing `activeContacts` into `buildHomeNodes` or computing it from the contacts already loaded by OrbMap.
   - The `CategoryNode` and `CreateCategoryNode` components can remain as files (they're used by CategoryTable route) but should no longer be imported/used by OrbMap.tsx. Remove their imports from OrbMap if present.

4. Wire '+' orb to PodCreateModal per D-13:
   - The current `CreateCategoryNode` ('+' orb on home view) should now open PodCreateModal instead of creating a category
   - In OrbMap: add `showCreatePod` state and `handlePodCreated` callback
   - Import `PodCreateModal` from `../pods/PodCreateModal`
   - The '+' node's click handler: `setShowCreatePod(true)`
   - Render: `<PodCreateModal isOpen={showCreatePod} onClose={() => setShowCreatePod(false)} onCreated={handlePodCreated} />`
   - `handlePodCreated`: invalidate pods cache, re-fetch pods to update the map, close modal
   - If the '+' node is a `CreateCategoryNode` component, either repurpose it or create the node inline in `buildHomeNodes`. The key is that clicking '+' on the home view creates a pod, not a category.
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/mrm && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ListNode.tsx contains `import { useNavigate } from 'react-router'` (or `from 'react-router-dom'`)
    - ListNode.tsx contains `navigate('/pod/'` or `navigate(\`/pod/\${`
    - ListNode.tsx does NOT contain `onListClick` prop callback invocation
    - ListNode.tsx contains capacity fraction display (`data.capacity`)
    - OrbMap.tsx does NOT contain `buildCategoryNodes` function
    - OrbMap.tsx does NOT contain `buildCategoryEdges` function
    - OrbMap.tsx does NOT contain `viewState` or `selectedListId` state (or category-drill equivalent)
    - OrbMap.tsx contains `PodCreateModal` import
    - OrbMap.tsx contains `showCreatePod` state
    - OrbMap.tsx buildHomeNodes includes `capacity` and `memberCount` in node data
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Pod orb clicks navigate to /pod/:id. Category-drill completely removed from OrbMap. Capacity indicator visible on capacity-limited pod orbs. '+' orb opens PodCreateModal. OrbMap is now a clean pod browser.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full Phase 12 flow</name>
  <files>n/a</files>
  <read_first>n/a</read_first>
  <action>Human verifies the complete Phase 12 flow end-to-end. No code changes in this task.</action>
  <what-built>Complete pods overhaul and categorization system: data model extensions, pending tray with swipe queue, categorization modal with required field enforcement, pod detail page, orb map navigation overhaul.</what-built>
  <how-to-verify>
    1. Start dev server: `pnpm dev`
    2. Enable demo mode (click "demo off" to "demo on" in nav)
    3. Dashboard: Verify pending tray widget appears showing pending count (if demo data has pending contacts)
    4. If pending exist: tap "Review" then swipe queue opens. Verify:
       - Cards show smart preview (name, email, company)
       - Swipe left or tap Skip sends card to back of queue
       - Tap Categorize opens modal with pod multi-select
       - Select 2+ pods and required fields sections appear for each pod
       - Primary pod radio appears when 2+ pods selected
       - Save disabled until all required fields answered
       - Save promotes contact to Active, removed from queue
    5. Map view: Click any pod orb and it navigates to /pod/:id (NOT category drill)
    6. Pod detail page: Shows settings (name, color, description, cadence, capacity), required fields list, sub-pods list, member list with scores
    7. Pod detail: Edit description and it saves on blur
    8. Map view: Click '+' orb and PodCreateModal opens, fill out form, save and new pod appears on map
    9. Verify capacity-limited pods show fraction label on orb
    10. Import a CSV file and verify contacts land as Pending (not Active)
  </how-to-verify>
  <verify>Human confirms all 10 verification steps pass</verify>
  <acceptance_criteria>
    - Pending tray widget visible on dashboard with count
    - Swipe queue opens and processes contacts
    - Categorization modal enforces required fields
    - Pod orb click navigates to /pod/:id
    - Pod detail page shows all four sections
    - Pod creation modal works from map
    - CSV imports land as Pending
  </acceptance_criteria>
  <done>Human has approved the complete Phase 12 flow. All categorization, pod management, and navigation changes verified.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` passes
- Orb map no longer has category-drill behavior
- Pod orb clicks navigate to /pod/:id
- '+' orb opens PodCreateModal
- Full categorization flow: widget to queue to modal to save to Active
- Pod detail page shows all sections
</verification>

<success_criteria>
Orb map is a clean pod browser with navigation to pod detail pages. Category-drill removed. Capacity indicators visible. Pod creation wired. Human has verified the complete Phase 12 flow end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/12-pods-overhaul-categorization/12-04-SUMMARY.md`
</output>
