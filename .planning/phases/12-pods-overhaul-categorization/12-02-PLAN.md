---
phase: 12-pods-overhaul-categorization
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/components/categorization/PendingTrayWidget.tsx
  - src/components/categorization/CategorizationQueue.tsx
  - src/components/categorization/CategorizationModal.tsx
  - src/components/dashboard/Dashboard.tsx
autonomous: true
requirements: [POD-05, POD-06, CAT-01, CAT-03, CAT-04, CAT-05, CAT-06]

must_haves:
  truths:
    - "Dashboard shows a pending tray widget with count of pending contacts"
    - "Tapping the widget opens a full-screen swipe queue overlay"
    - "Swipe queue shows contact cards one at a time with smart preview"
    - "Swiping left or tapping Skip sends contact to back of queue"
    - "Tapping Categorize opens all-in-one categorization modal"
    - "Categorization modal shows pod multi-select with inline required fields"
    - "Save is disabled until all required fields for all selected pods are answered"
    - "Saving promotes contact to Active status with pod assignments and primary pod"
    - "A timeline note is written on categorization (CAT-06)"
  artifacts:
    - path: "src/components/categorization/PendingTrayWidget.tsx"
      provides: "Dashboard widget showing pending count + preview names"
    - path: "src/components/categorization/CategorizationQueue.tsx"
      provides: "Full-screen swipe queue overlay with card stack"
    - path: "src/components/categorization/CategorizationModal.tsx"
      provides: "All-in-one categorization: pod select, required fields, primary pod, save"
  key_links:
    - from: "PendingTrayWidget.tsx"
      to: "CategorizationQueue.tsx"
      via: "Review button opens queue overlay"
      pattern: "setShowQueue.*true"
    - from: "CategorizationQueue.tsx"
      to: "CategorizationModal.tsx"
      via: "Categorize button on card opens modal for that contact"
      pattern: "setCategorizing.*true|setActiveContact"
    - from: "CategorizationModal.tsx"
      to: "airtable.ts"
      via: "Save calls updateContact + createInteraction"
      pattern: "updateContact.*status.*Active"
    - from: "Dashboard.tsx"
      to: "PendingTrayWidget.tsx"
      via: "Widget rendered in dashboard layout"
      pattern: "PendingTrayWidget"
---

<objective>
Build the pending categorization tray, Tinder-style swipe queue, and all-in-one categorization modal. This is the intake workflow centerpiece.

Purpose: Records from external sources (CSV import) land as Pending and must be categorized before entering the CRM. This plan builds the full categorization flow.
Output: Three new components (widget, queue, modal) wired into the dashboard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-pods-overhaul-categorization/12-CONTEXT.md
@.planning/phases/12-pods-overhaul-categorization/12-RESEARCH.md
@.planning/phases/12-pods-overhaul-categorization/12-01-SUMMARY.md

@src/components/dashboard/Dashboard.tsx
@src/lib/airtable.ts
@src/lib/types.ts
@src/lib/escapeStack.ts
@src/lib/fieldConfig.ts
@src/components/records/PodFieldsWidget.tsx
@docs/design-system.md

<interfaces>
<!-- From 12-01 (data layer) — these will exist when this plan runs -->
export async function getPendingContacts(): Promise<Contact[]>
export async function createPod(data: ...): Promise<Pod>
export async function updatePod(id: string, data: ...): Promise<Pod>

<!-- Contact now has: -->
primary_list_id: string | null
cadence_override: Cadence | null

<!-- Pod now has: -->
description: string | null
capacity: number | null

<!-- Existing functions used by categorization -->
export async function getActiveContacts(): Promise<Contact[]>
export async function updateContact(id: string, fields: Partial<ContactFields>): Promise<Contact>
export async function createInteraction(fields: InteractionInput): Promise<Interaction>
export function invalidateContactsCache(): void
export async function getFieldConfigs(): Promise<FieldConfig[]>
export async function getPods(): Promise<Pod[]>

<!-- FieldConfig interface (from fieldConfig.ts) -->
export interface FieldConfig {
  id: string
  name: string
  field_type: 'text' | 'multiline' | 'number' | 'select' | 'date' | 'checkbox'
  scope_type: RelationshipType | null
  scope_pod_id: string | null
  required: boolean
  options: string[] | null
  table_id: string | null
}

<!-- escapeStack hook -->
export function useEscape(callback: () => void): void
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PendingTrayWidget + CategorizationQueue (swipe card stack)</name>
  <files>src/components/categorization/PendingTrayWidget.tsx, src/components/categorization/CategorizationQueue.tsx, src/components/dashboard/Dashboard.tsx</files>
  <read_first>src/components/dashboard/Dashboard.tsx, src/lib/airtable.ts, src/lib/escapeStack.ts, docs/design-system.md, src/components/dashboard/WrappedCard.tsx (for widget card pattern)</read_first>
  <action>
1. Create `src/components/categorization/PendingTrayWidget.tsx`:
   - Props: `pendingContacts: Contact[]`, `onReview: () => void`
   - Shows pending count as a large number (Fraunces serif, like WrappedCard stats)
   - Shows up to 3 preview names below the count in secondary text
   - Gentle urgency per D-03: no color escalation, no badges. Muted count, text-secondary color.
   - "Review" button triggers `onReview` callback
   - If `pendingContacts.length === 0`, render muted "0 pending" state (no CTA button)
   - Card style: same PANEL pattern as other dashboard widgets (`var(--surface-panel)`, `backdrop-filter: var(--panel-blur)`, `var(--panel-radius)`)

2. Create `src/components/categorization/CategorizationQueue.tsx`:
   - Full-screen overlay (`position: fixed, inset: 0, z-index: 200, background: rgba(0,0,0,0.5)`)
   - Props: `contacts: Contact[]`, `onClose: () => void`, `onCategorized: (contactId: string) => void`
   - Local state: `queue` (array of contacts, initially = props.contacts), `currentIndex` (starts at 0)
   - Card stack: render top 3 cards. Top card (index 0) is interactive, cards 1-2 are offset behind (translateY(8px), translateY(16px), scale(0.97/0.94), opacity(0.7/0.4))
   - Smart preview per D-08: card shows whatever fields exist on the contact:
     * If `contact.intel_notes` populated and `contact.name === 'Brain Dump'` → show intel_notes as primary content
     * Otherwise → show name (large, Fraunces), email, company/role, source indicator
     * Show `contact.notes` if populated
   - Swipe gesture per D-04: pointer-based drag on top card only
     * `onPointerDown`: capture `startX`, set `dragging = true`, call `setPointerCapture`
     * `onPointerMove`: if dragging, set `dragX = clientX - startX`
     * `onPointerUp`: if `dragX < -80` → skip (animate card off-screen left, then requeue at end per D-05). Otherwise snap back.
     * Card style while dragging: `transform: translateX(${dragX}px) rotate(${dragX * 0.04}deg)`, `transition: dragging ? 'none' : 'transform 0.2s cubic-bezier(0.87, 0, 0.13, 1)'`
   - Two action buttons below card: "Skip" (left arrow icon) and "Categorize" (checkmark icon)
   - Skip: move current contact to end of queue array. Advance to next card.
   - Categorize: set `categorizingContact` state to current contact, render CategorizationModal
   - When CategorizationModal calls `onCategorized(id)`: remove contact from queue, advance to next. Also call props.onCategorized.
   - When queue is empty: show "All caught up" message with close button
   - Use `useEscape(onClose)` for Escape key handling
   - Close button (X) in top-right corner

3. Wire into Dashboard.tsx:
   - Import PendingTrayWidget and CategorizationQueue
   - In the Dashboard data loading (useEffect), also call `getPendingContacts()` alongside existing data fetches
   - Store `pendingContacts` in state
   - Add `showQueue` boolean state
   - Render `<PendingTrayWidget pendingContacts={pending} onReview={() => setShowQueue(true)} />` as the FIRST widget in the dashboard grid (top of the widget area, before pod cards)
   - Render `{showQueue && <CategorizationQueue contacts={pending} onClose={() => setShowQueue(false)} onCategorized={(id) => { setPending(prev => prev.filter(c => c.id !== id)); invalidateContactsCache(); }} />}`
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/mrm && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PendingTrayWidget.tsx exists and contains `pendingContacts` prop usage
    - PendingTrayWidget.tsx contains `onReview` callback
    - CategorizationQueue.tsx exists and contains `onPointerDown` and `onPointerMove` and `onPointerUp`
    - CategorizationQueue.tsx contains `dragX < -80` threshold for skip gesture
    - CategorizationQueue.tsx contains `useEscape` import from escapeStack
    - CategorizationQueue.tsx contains `intel_notes` check for brain dump smart preview
    - Dashboard.tsx contains `PendingTrayWidget` import
    - Dashboard.tsx contains `getPendingContacts` call
    - Dashboard.tsx contains `CategorizationQueue` conditional render
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Dashboard shows pending tray widget. Tapping Review opens swipe queue. Cards show smart preview. Swipe left skips. Queue overlay functional.</done>
</task>

<task type="auto">
  <name>Task 2: CategorizationModal — all-in-one pod assignment with required field enforcement</name>
  <files>src/components/categorization/CategorizationModal.tsx, src/components/categorization/CategorizationQueue.tsx</files>
  <read_first>src/components/records/PodFieldsWidget.tsx, src/lib/fieldConfig.ts, src/lib/airtable.ts (updateContact, createInteraction, getFieldConfigs, getPods, getActiveContacts), src/components/categorization/CategorizationQueue.tsx</read_first>
  <action>
1. Create `src/components/categorization/CategorizationModal.tsx`:
   - Props: `contact: Contact`, `pods: Pod[]`, `fieldConfigs: FieldConfig[]`, `onCategorized: (contactId: string) => void`, `onSkip: () => void`, `onClose: () => void`
   - Layout per D-06: Single scrollable screen. Top section = pod multi-select. Below = dynamic required field sections per selected pod. Bottom = primary pod selector + notes + action buttons.

   **Pod multi-select section:**
   - Grid of pod chips/buttons, each showing pod color dot + name
   - Toggle selection on click. Selected pods get highlighted border/background using pod color.
   - When a pod with `pod.capacity` is selected, immediately check member count:
     ```
     const activeContacts = await getActiveContacts()
     const count = activeContacts.filter(c => c.status === 'Active' && c.list_ids.includes(pod.id)).length
     if (pod.capacity && count >= pod.capacity) → show inline warning below pod chip
     ```
     Warning text: "{pod.name} is at {count}/{capacity} -- consider reviewing." with [Add Anyway] link to dismiss warning. Per D-09.

   **Required fields section (progressive disclosure per D-06):**
   - For each selected pod, filter `fieldConfigs` where `fc.scope_pod_id === podId`
   - Group required fields under a pod-colored header: "{Pod Name} Fields"
   - Render each field based on `field_type`: text → input, multiline → textarea, number → number input, select → dropdown with `fc.options`, date → date input, checkbox → checkbox
   - Store answers in local state: `answers: Record<string, unknown>` keyed by field config name
   - Required fields (fc.required === true) show a red asterisk. Optional fields show normally.
   - Sections appear/disappear as pods are toggled — progressive disclosure.

   **Primary pod radio (per D-06):**
   - Only visible when 2+ pods selected
   - Radio button group: one per selected pod. Label = pod name with color dot.
   - Auto-select first pod if only one selected.
   - Stored as `primaryPodId` state.

   **Notes textarea:**
   - Optional contextual notes field at bottom

   **Save validation per D-07:**
   ```
   const allRequiredAnswered = selectedPodIds.every(podId => {
     const podRequired = fieldConfigs.filter(fc => fc.scope_pod_id === podId && fc.required)
     return podRequired.every(fc => {
       const val = answers[fc.name]
       return val !== null && val !== undefined && val !== ''
     })
   })
   const canSave = allRequiredAnswered && primaryPodId && selectedPodIds.length > 0 && !saving
   ```
   Save button: `disabled={!canSave}`. Muted styling when disabled.

   **Save sequence:**
   1. `await updateContact(contact.id, { status: 'Active', list_ids: selectedPodIds, primary_list_id: primaryPodId, custom_fields: { ...contact.custom_fields, ...answers } })`
   2. Build categorization summary: `const podNames = selectedPodIds.map(id => pods.find(p => p.id === id)?.name ?? id)`
   3. `await createInteraction({ contact_id: contact.id, type: 'note', date: new Date().toISOString().split('T')[0], notes: 'Categorized into: ' + podNames.join(', ') + '. Primary: ' + primaryPodName + '.' + (notes ? ' Notes: ' + notes : '') })`
   4. `invalidateContactsCache()`
   5. Call `onCategorized(contact.id)`

   **Skip button per D-05/D-07:** Calls `onSkip()` — sends contact back to queue (handled by CategorizationQueue).

   **Close button:** Calls `onClose()`.

   Use `useEscape(onClose)` for escape key.

2. Wire CategorizationModal into CategorizationQueue.tsx:
   - CategorizationQueue loads `pods` and `fieldConfigs` once on mount (not per card) per Research pitfall 4
   - When user taps "Categorize" on a card: set `categorizingContact = currentContact`
   - Render `{categorizingContact && <CategorizationModal contact={categorizingContact} pods={pods} fieldConfigs={fieldConfigs} onCategorized={handleCategorized} onSkip={handleSkipFromModal} onClose={() => setCategorizingContact(null)} />}`
   - `handleCategorized`: remove contact from queue, call props.onCategorized, clear categorizingContact
   - `handleSkipFromModal`: move contact to end of queue, advance, clear categorizingContact
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/mrm && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CategorizationModal.tsx exists and contains `selectedPodIds` state
    - CategorizationModal.tsx contains `allRequiredAnswered` validation logic
    - CategorizationModal.tsx contains `fc.scope_pod_id === podId && fc.required` filter
    - CategorizationModal.tsx contains `updateContact` call with `status: 'Active'`
    - CategorizationModal.tsx contains `createInteraction` call with `type: 'note'` and `Categorized into:`
    - CategorizationModal.tsx contains `primaryPodId` state
    - CategorizationModal.tsx contains `disabled={!canSave}` or equivalent disabled prop on Save button
    - CategorizationModal.tsx contains `invalidateContactsCache` call
    - CategorizationModal.tsx contains capacity warning text pattern `consider reviewing`
    - CategorizationQueue.tsx imports and renders CategorizationModal
    - CategorizationQueue.tsx loads `fieldConfigs` on mount (not per card)
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Full categorization flow works: pod multi-select with dynamic required fields, primary pod radio, required field enforcement (save disabled), capacity soft-cap warning, timeline entry on save, skip returns to queue. All wired into the swipe queue.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes
- Dashboard renders PendingTrayWidget
- CategorizationQueue has pointer-based swipe gesture
- CategorizationModal enforces required fields before save
- Save writes both contact update and interaction note
- Capacity warning shows for at-capacity pods
</verification>

<success_criteria>
Complete pending categorization workflow: dashboard widget showing pending count, Tinder-style swipe queue with smart preview and gesture support, all-in-one categorization modal with pod selection + dynamic required fields + primary pod + capacity warnings + timeline entry. Save promotes to Active.
</success_criteria>

<output>
After completion, create `.planning/phases/12-pods-overhaul-categorization/12-02-SUMMARY.md`
</output>
