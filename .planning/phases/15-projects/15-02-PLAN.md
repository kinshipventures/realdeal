---
phase: 15-projects
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/App.tsx
  - src/components/projects/ProjectsPage.tsx
  - src/components/projects/ProjectDetailPage.tsx
  - src/components/projects/CreateProjectModal.tsx
  - src/components/projects/AddToProjectModal.tsx
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-05, PROJ-06, NAV-01, NAV-03, NAV-04]

must_haves:
  truths:
    - "User can see all projects as cards on /projects"
    - "User can create a new project via modal"
    - "User can open a project and see attached contacts, opportunities, and notes"
    - "User can attach/remove contacts and opportunities from a project"
    - "User can add notes to a project"
    - "Nav pill includes Projects entry"
    - "Projects route works at /projects and /projects/:id"
  artifacts:
    - path: "src/components/projects/ProjectsPage.tsx"
      provides: "Card grid of all projects"
      min_lines: 60
    - path: "src/components/projects/ProjectDetailPage.tsx"
      provides: "Single project view with tabs"
      min_lines: 100
    - path: "src/components/projects/CreateProjectModal.tsx"
      provides: "Project creation modal"
      min_lines: 40
    - path: "src/components/projects/AddToProjectModal.tsx"
      provides: "Search/picker modal for attaching records or opportunities"
      min_lines: 60
    - path: "src/App.tsx"
      provides: "Routes and nav for Projects"
      contains: "projects"
  key_links:
    - from: "src/App.tsx"
      to: "ProjectsPage, ProjectDetailPage"
      via: "Route elements"
      pattern: "Route.*path.*projects"
    - from: "ProjectsPage.tsx"
      to: "getProjects, createProject"
      via: "import from airtable"
      pattern: "import.*getProjects.*airtable"
    - from: "ProjectDetailPage.tsx"
      to: "addRecordToProject, removeRecordFromProject, addProjectNote"
      via: "import from airtable"
      pattern: "import.*addRecordToProject.*airtable"
---

<objective>
Projects UI: landing page with card grid, detail page with tabbed layout, creation modal, and attach records modal. Plus nav pill entry and routes.

Purpose: The core project experience — users can create, view, and manage projects as lightweight initiative containers distinct from pipelines.
Output: Full project UI with navigation wired into the app.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-projects/15-CONTEXT.md
@.planning/phases/15-projects/15-01-SUMMARY.md

@src/App.tsx
@src/components/pipelines/PipelinesPage.tsx
@src/components/pipelines/AddToPipelineModal.tsx
@src/components/empty/EmptyState.tsx
@docs/design-system.md

<interfaces>
From src/lib/types.ts:
```typescript
export interface Project {
  id: string
  name: string
  description: string | null
  owner: string | null
  relationship_ids: string[]
  opportunity_ids: string[]
  notes: string | null
  created_at: string
}
```

From src/lib/airtable.ts (after Plan 01):
```typescript
export function getProjects(): Promise<Project[]>
export function createProject(name: string, description?: string): Promise<Project>
export function updateProject(id: string, data: Partial<Pick<Project, 'name' | 'description' | 'owner'>>): Promise<Project>
export function addRecordToProject(projectId: string, recordId: string): Promise<Project>
export function removeRecordFromProject(projectId: string, recordId: string): Promise<Project>
export function addOpportunityToProject(projectId: string, opportunityId: string): Promise<Project>
export function removeOpportunityFromProject(projectId: string, opportunityId: string): Promise<Project>
export function addProjectNote(projectId: string, note: string): Promise<void>
export function invalidateProjectsCache(): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProjectsPage card grid + CreateProjectModal + nav/routes</name>
  <files>src/components/projects/ProjectsPage.tsx, src/components/projects/CreateProjectModal.tsx, src/App.tsx</files>
  <read_first>
    - src/App.tsx (full file — nav pill structure, routes, imports, navItems array or inline nav)
    - src/components/pipelines/PipelinesPage.tsx (pattern for top-level module page)
    - src/components/empty/EmptyState.tsx (empty state component API — props: heading, ctaLabel, onCta, ghosts)
    - src/components/pipelines/CreatePipelineModal.tsx (modal creation pattern)
    - docs/design-system.md (design tokens, typography, spacing)
  </read_first>
  <action>
**App.tsx changes (per D-01):**

1. Add import: `import { ProjectsPage } from './components/projects/ProjectsPage'` and `import { ProjectDetailPage } from './components/projects/ProjectDetailPage'`
2. Add `isProjects` boolean: `const isProjects = location.pathname.startsWith('/projects')`
3. Add "Projects" nav pill entry in both mobile bottom nav AND desktop floating pill nav, after the Pipelines entry. Use a folder/briefcase icon from lucide-react (e.g., `FolderKanban` or `Briefcase`). Follow the exact same structure as the Pipelines entry: aria-current, color logic using `isProjects`, label "Projects".
4. Update the `isPulse` logic to exclude `/projects`: `const isPulse = !isMap && !isContacts && !isPipelines && !isProjects && location.pathname === '/'`
5. Add routes inside the `<Route element={<AppShell />}>` block:
   - `<Route path="projects" element={<ProjectsPage />} />`
   - `<Route path="projects/:id" element={<ProjectDetailPage />} />`

**ProjectsPage.tsx (per D-01, D-02, D-03, D-04):**

Create `src/components/projects/ProjectsPage.tsx`:

- Load all projects via `getProjects()` on mount
- Display as card grid (CSS Grid, 3 columns on large screens, 2 on medium, 1 on small)
- Each project card shows:
  - Project name (font-family: var(--font-serif), fontWeight 600)
  - Description snippet (truncated to 2 lines, fontSize 13, color: var(--color-text-secondary))
  - Footer row: contact count (`{n} contacts`) and opportunity count (`{n} opportunities`) in tertiary text
- Card styling: background `rgba(255,255,255,0.92)`, border `1px solid rgba(0,0,0,0.07)`, borderRadius 12, padding 20, cursor pointer
- Card hover: slightly elevated shadow `0 2px 8px rgba(0,0,0,0.08)`
- Click card navigates to `/projects/:id` via `useNavigate()`
- "+" button at top right creates new project (opens CreateProjectModal)
- Page header: "Projects" in serif font, uppercase label style matching other pages
- Empty state (per D-04): Use `EmptyState` component with heading "No projects yet", ctaLabel "Create your first project". After the EmptyState, add a small paragraph: "Organize podcast outreach, philanthropy campaigns, SPV initiatives, events, and more."

**CreateProjectModal.tsx (per D-03):**

Create `src/components/projects/CreateProjectModal.tsx`:

Props: `{ open: boolean, onClose: () => void, onCreate: (project: Project) => void }`

- Overlay + centered modal (follow CreatePipelineModal pattern)
- Fields: name (required, text input), description (optional, textarea)
- "Create" button calls `createProject(name, description)`, then `onCreate(project)`, then `onClose()`
- Escape key closes via `useEscape` from `escapeStack.ts`
- Focus trap on name input on mount
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/mrm && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/projects/ProjectsPage.tsx exists with min 80 lines
    - src/components/projects/CreateProjectModal.tsx exists with min 40 lines
    - App.tsx contains `import { ProjectsPage }` and `import { ProjectDetailPage }`
    - App.tsx contains `path="projects"` route and `path="projects/:id"` route
    - App.tsx contains `isProjects` variable
    - App.tsx contains "Projects" label in nav pill
    - App.tsx isPulse logic excludes isProjects
    - ProjectsPage.tsx imports getProjects from airtable
    - ProjectsPage.tsx uses EmptyState component for zero-project state
    - CreateProjectModal.tsx imports createProject from airtable
    - pnpm build passes
  </acceptance_criteria>
  <done>Projects landing page shows card grid, creation modal works, nav pill includes Projects, routes resolve at /projects and /projects/:id</done>
</task>

<task type="auto">
  <name>Task 2: ProjectDetailPage with tabs (Contacts, Opportunities, Notes) + AddToProjectModal</name>
  <files>src/components/projects/ProjectDetailPage.tsx, src/components/projects/AddToProjectModal.tsx</files>
  <read_first>
    - src/components/pipelines/AddToPipelineModal.tsx (search/picker modal pattern — search input, results list, multi-select, confirm)
    - src/components/records/RecordPage.tsx (full page layout pattern, header with inline edit)
    - src/lib/airtable.ts (getContacts, getOpportunities — for populating picker modals)
    - src/lib/types.ts (Contact, Opportunity, Project interfaces)
  </read_first>
  <action>
**ProjectDetailPage.tsx (per D-05, D-06, D-07, D-08, D-09):**

Create `src/components/projects/ProjectDetailPage.tsx`:

- Load project by ID from `useParams()` — call `getProjects()` then `.find(p => p.id === id)`
- Load contacts via `getContacts()`, filter to `project.relationship_ids`
- Load opportunities via `getOpportunities()`, filter to `project.opportunity_ids`
- Load interactions for notes tab: `getInteractions()` filtered to `type === 'project_event'` and `event_detail` containing this project's ID with `action === 'project_note'`

**Header:**
- Project name displayed large (font-family: var(--font-serif), fontSize 24, fontWeight 700)
- Description below in secondary text (fontSize 14, color: var(--color-text-secondary))
- Inline edit on click for both name and description (consistent with PodDetailPage pattern per D-12 from Phase 12 decisions) — blur saves via `updateProject()`
- Back button (arrow-left icon) navigates to `/projects`

**Tab bar:**
- Three tabs: "Contacts", "Opportunities", "Notes"
- Simple text tabs with bottom border indicator on active tab
- Active: fontWeight 600, borderBottom 2px solid var(--color-text-primary)
- Inactive: fontWeight 400, color var(--color-text-secondary)

**Contacts tab (per D-08, D-09):**
- List of contacts with name, role/company, and a "Remove" button (X icon) on each row
- Row click navigates to `/contact/:id` (per NAV-01)
- "+" button at top opens AddToProjectModal in "contacts" mode
- Remove calls `removeRecordFromProject(projectId, contactId)` and refreshes

**Opportunities tab (per D-08, D-09):**
- List of opportunities with name, stage info, and "Remove" button
- Row click navigates to `/pipelines?opportunity=:id` (per NAV-02)
- "+" button at top opens AddToProjectModal in "opportunities" mode
- Remove calls `removeOpportunityFromProject(projectId, oppId)` and refreshes

**Notes tab (per D-07):**
- Chronological feed, most recent first
- Each note: timestamp (formatRelativeTime), note text
- Add note input at top: text input + "Add" button
- Submit calls `addProjectNote(projectId, noteText)` and refreshes the list
- Notes are Interaction records with `type === 'project_event'` and `event_detail.action === 'project_note'`

**AddToProjectModal.tsx (per D-09):**

Create `src/components/projects/AddToProjectModal.tsx`:

Props: `{ open: boolean, onClose: () => void, projectId: string, mode: 'contacts' | 'opportunities', existingIds: string[], onAdded: () => void }`

- Overlay + centered modal (same as AddToPipelineModal pattern)
- Search input at top — filters the list as user types
- For contacts mode: load all contacts via `getContacts()`, filter out `existingIds`, search by name/company/email
- For opportunities mode: load all opportunities via `getOpportunities()`, filter out `existingIds`, search by name
- Each result row has a checkbox for multi-select
- "Add Selected" button at bottom:
  - Contacts mode: calls `addRecordToProject(projectId, recordId)` for each selected
  - Opportunities mode: calls `addOpportunityToProject(projectId, oppId)` for each selected
- After adding, calls `onAdded()` callback and closes
- Escape closes via `useEscape`
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/mrm && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/projects/ProjectDetailPage.tsx exists with min 150 lines
    - src/components/projects/AddToProjectModal.tsx exists with min 60 lines
    - ProjectDetailPage.tsx uses useParams to get project ID
    - ProjectDetailPage.tsx imports addRecordToProject, removeRecordFromProject, addProjectNote, updateProject from airtable
    - ProjectDetailPage.tsx has three tabs: grep for "Contacts" AND "Opportunities" AND "Notes" as tab labels
    - ProjectDetailPage.tsx navigates to /contact/ on row click (NAV-01)
    - ProjectDetailPage.tsx contains back navigation to /projects
    - AddToProjectModal.tsx imports getContacts and getOpportunities
    - AddToProjectModal.tsx accepts mode prop of type 'contacts' | 'opportunities'
    - pnpm build passes
  </acceptance_criteria>
  <done>Project detail page shows header with inline edit, three tabbed sections (Contacts/Opportunities/Notes), attach/remove/add-note all functional, navigation to records and pipelines works from project context</done>
</task>

</tasks>

<verification>
- `pnpm build` passes with zero errors
- Navigate to `/projects` — card grid visible (or empty state)
- Click "+" — create project modal opens, create succeeds
- Click project card — detail page loads at `/projects/:id`
- Tab through Contacts/Opportunities/Notes — each tab renders
- Attach a contact — appears in list, can be removed
- Add a note — appears in notes feed
- Nav pill shows "Projects" entry with active state on /projects routes
</verification>

<success_criteria>
- Projects page renders at /projects with card grid or empty state
- Project creation modal works end-to-end
- Project detail page shows at /projects/:id with tabs
- Contacts and opportunities can be attached and removed
- Notes can be added and display chronologically
- Nav pill includes Projects with correct active state
- All navigation preserves context (no full page reloads)
</success_criteria>

<output>
After completion, create `.planning/phases/15-projects/15-02-SUMMARY.md`
</output>
