---
phase: 02-visual-redesign
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/dashboard/Dashboard.tsx
  - src/App.tsx
  - src/components/contacts/ContactPanel.tsx
  - src/components/contacts/ContactDetail.tsx
  - src/components/contacts/ContactCard.tsx
  - docs/design-system.md
autonomous: false
requirements: [VIS-02, VIS-03]

must_haves:
  truths:
    - "Dashboard has a green header band at the top containing the equity ring and stats"
    - "Equity ring uses white/white-alpha colors on the green band (not orange-to-purple)"
    - "All dashboard text on the green band is white"
    - "Pod cards use surface-panel background with solid left border in pod color"
    - "Nav pill active state is green with white text"
    - "Background is clean near-white (#f8f8f6), no lavender/peach/blue atmospheric gradient"
    - "Section headings use serif font (Playfair Display)"
    - "Contact panels use new typography and token colors"
  artifacts:
    - path: "src/components/dashboard/Dashboard.tsx"
      provides: "Green header band, tokenized inline styles, updated equity ring gradient, pod cards"
      contains: "var(--header-band-bg)"
    - path: "src/App.tsx"
      provides: "Clean background, green nav active state"
      contains: "var(--color-brand)"
    - path: "docs/design-system.md"
      provides: "Updated design system documentation reflecting Trolley-aligned tokens"
      contains: "Playfair Display"
  key_links:
    - from: "src/components/dashboard/Dashboard.tsx"
      to: "src/styles/globals.css"
      via: "var(--header-band-bg), var(--surface-panel), var(--color-text-primary)"
      pattern: "var\\(--header-band-bg\\)"
    - from: "src/App.tsx"
      to: "src/styles/globals.css"
      via: "var(--color-brand), var(--color-bg)"
      pattern: "var\\(--color-brand\\)"
---

<objective>
Dashboard green header band, full surface token migration, nav redesign, contact panel refresh, and design system documentation.

Purpose: This is the "Trolley treatment" — the 5 deltas that make the app demo-ready for Gwyneth. Tokens from plan 01 are consumed here across all surfaces.
Output: Dashboard with green header band, tokenized styles everywhere, green nav, refreshed contact panels, updated design-system.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-redesign/02-CONTEXT.md
@.planning/phases/02-visual-redesign/02-RESEARCH.md
@.planning/phases/02-visual-redesign/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard green header band + full token migration</name>
  <files>src/components/dashboard/Dashboard.tsx</files>
  <read_first>
    - src/components/dashboard/Dashboard.tsx (full file — 568 lines, all inline styles with magic values, PANEL constant, EquityRing, StatBlock, PodCard, FocusCard, OverdueRow, DormantRow sub-components)
    - src/styles/globals.css (token values defined in plan 01 — --header-band-bg, --surface-panel, --color-text-primary, --panel-radius, etc.)
    - .planning/phases/02-visual-redesign/02-RESEARCH.md (green header band pattern, equity ring on green, pod card treatment, text contrast pitfalls)
  </read_first>
  <action>
**PANEL constant update** — replace the glass overlay with tokenized surface:
```tsx
const PANEL: React.CSSProperties = {
  background: 'var(--surface-panel)',
  backdropFilter: 'var(--panel-blur)',
  WebkitBackdropFilter: 'var(--panel-blur)',
  border: 'var(--surface-panel-border)',
  borderRadius: 'var(--panel-radius)',
}
```

**Green header band (D-10, D-18)** — wrap the top row (equity ring + stats) in a green band. Replace the current `<div style={{ maxWidth: 960, margin: '0 auto', padding: '40px 24px 80px' }}>` structure with a two-section layout:

Section 1 — Green band:
```tsx
<div style={{ background: 'var(--header-band-bg)', borderRadius: '0 0 20px 20px' }}>
  <div style={{ maxWidth: 960, margin: '0 auto', padding: '28px 24px 32px' }}>
    {/* Top row: Score + Stats — all text white on green */}
    <div style={{ display: 'flex', gap: 24, alignItems: 'flex-start' }}>
      {/* Equity score ring — NO PANEL wrapper, directly on green */}
      <div style={{ display: 'flex', alignItems: 'center', gap: 24, flex: '0 0 auto' }}>
        ...equity ring + score/label...
      </div>
      {/* Stats — light panel on green */}
      <div style={{ background: 'rgba(255,255,255,0.15)', borderRadius: 'var(--panel-radius)', padding: '20px 24px', flex: 1 }}>
        ...stat blocks...
      </div>
    </div>
  </div>
</div>
```

Section 2 — Rest of dashboard on light background:
```tsx
<div style={{ maxWidth: 960, margin: '0 auto', padding: '24px 24px 80px' }}>
  {/* Pod cards, focus, overdue, dormant — all on light bg */}
</div>
```

**Text colors on green band** (pitfall 2 — WCAG contrast):
- Score number: `color: '#ffffff'` (was `rgba(0,0,0,0.85)`)
- Score label: `color: 'rgba(255,255,255,0.70)'` (was `rgba(0,0,0,0.45)`)
- Remove `...PANEL` spread from equity ring wrapper — it sits directly on green, no glass panel

**EquityRing component update** (D-21, pitfall 1):
- Ghost track: change `stroke="rgba(0,0,0,0.06)"` to `stroke="rgba(255,255,255,0.20)"`
- Gradient: replace orange-to-purple with white gradient for green band context:
```tsx
<linearGradient id="equityGradient" x1="0%" y1="0%" x2="100%" y2="100%">
  <stop offset="0%" stopColor="rgba(255,255,255,0.95)" />
  <stop offset="100%" stopColor="rgba(255,255,255,0.60)" />
</linearGradient>
```

**StatBlock component update** — on green band, all text flips to white:
- Value color: `'#ffffff'` (was `rgba(0,0,0,0.82)`)
- Accent value (overdue): keep `hsla(20, 80%, 45%, 0.80)` — orange on green is fine
- Label color: `'rgba(255,255,255,0.55)'` (was `rgba(0,0,0,0.35)`)

**PodCard component update** (D-04, D-20):
- Remove `...PANEL` spread — use explicit tokenized styles:
```tsx
style={{
  background: 'var(--surface-panel)',
  border: '1px solid rgba(0,0,0,0.07)',
  borderLeft: `4px solid ${color}`,
  borderRadius: 12,
  padding: '16px 20px',
  minWidth: 160,
  flexShrink: 0,
}}
```
- Pod name: add `fontFamily: 'var(--font-serif)'` per D-06
- Replace magic text colors with token vars:
  - `rgba(0,0,0,0.82)` → `'var(--color-text-primary)'`
  - `rgba(0,0,0,0.45)` → `'var(--color-text-secondary)'`
  - `rgba(0,0,0,0.25)` → `'var(--color-text-tertiary)'`

**Section headings** (D-06) — add `fontFamily: 'var(--font-serif)'` to:
- "needs attention" heading (line ~238): add `fontFamily: 'var(--font-serif)'`
- "today's focus" label (line ~221): add `fontFamily: 'var(--font-serif)'`, change to `fontSize: 13, fontWeight: 700` (serif heading treatment, not uppercase label)

**FocusCard update** — refresh within new design language:
- Remove warm orange tint: `background: 'rgba(255, 245, 235, 0.6)'` → `background: 'var(--surface-panel)'`
- Change top border accent: `borderTop: '2px solid #FFB547'` → `borderTop: '2px solid var(--color-brand)'`
- Replace magic text colors with token vars

**OverdueRow, DormantRow** — replace magic color values with token vars:
- `rgba(0,0,0,0.82)` → `'var(--color-text-primary)'`
- `rgba(0,0,0,0.45)` or `rgba(0,0,0,0.50)` → `'var(--color-text-secondary)'`
- `rgba(0,0,0,0.28)` or `rgba(0,0,0,0.30)` or `rgba(0,0,0,0.35)` → `'var(--color-text-tertiary)'`
- `rgba(0,0,0,0.72)` or `rgba(0,0,0,0.65)` → `'var(--color-text-primary)'`
- Keep `hsla(20, 80%, 45%, 0.80)` for overdue accent — it's a semantic color, not a shared token

**DormantRow action buttons** — tokenize:
- Button background: `rgba(0,0,0,0.04)` → keep (too specific for token)
- Button text: `rgba(0,0,0,0.45)` → `'var(--color-text-secondary)'`

Do NOT convert inline styles to Tailwind classes — the file uses inline styles and that pattern stays consistent per RESEARCH.md anti-patterns.
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/kinshipbrain && grep -c "var(--header-band-bg)" src/components/dashboard/Dashboard.tsx && grep -c "var(--color-text-primary)" src/components/dashboard/Dashboard.tsx && grep -c "var(--font-serif)" src/components/dashboard/Dashboard.tsx && grep -c "rgba(0,0,0,0.82)" src/components/dashboard/Dashboard.tsx && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.tsx contains `var(--header-band-bg)` (green band wrapper)
    - Dashboard.tsx contains `var(--surface-panel)` in PANEL constant
    - Dashboard.tsx contains `var(--panel-blur)` in PANEL constant
    - Dashboard.tsx contains `var(--color-text-primary)` (at least 3 occurrences)
    - Dashboard.tsx contains `var(--font-serif)` (at least 2 occurrences — headings)
    - Dashboard.tsx does NOT contain `rgba(245,244,240,0.88)` (old PANEL bg)
    - Dashboard.tsx EquityRing gradient contains `rgba(255,255,255,0.95)` (white gradient on green)
    - Dashboard.tsx EquityRing track contains `rgba(255,255,255,0.20)` (white track on green)
    - Dashboard.tsx does NOT contain `#FF6B4A` or `#7B61FF` (old orange-to-purple gradient)
    - Dashboard.tsx score number on green band uses `#ffffff` color
    - Dashboard.tsx FocusCard uses `var(--surface-panel)` not `rgba(255, 245, 235, 0.6)`
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>Dashboard has green header band with white text on green. Equity ring uses white gradient on green background. All shared magic values replaced with CSS custom property references. Section headings use serif font. Pod cards, focus cards, overdue rows, dormant rows all use tokenized styles.</done>
</task>

<task type="auto">
  <name>Task 2: App shell (background + nav green) + contact panels token migration</name>
  <files>src/App.tsx, src/components/contacts/ContactPanel.tsx, src/components/contacts/ContactDetail.tsx, src/components/contacts/ContactCard.tsx, docs/design-system.md</files>
  <read_first>
    - src/App.tsx (BG gradient mesh, nav pill button styles — isActive background/color/boxShadow)
    - src/components/contacts/ContactPanel.tsx (panel background, text colors, search input styles)
    - src/components/contacts/ContactDetail.tsx (header, section headings, field labels, interaction colors, RING_COLORS, SegmentedEquityRing)
    - src/components/contacts/ContactCard.tsx (row component, text colors)
    - docs/design-system.md (current glass-era design system — will be rewritten)
    - src/styles/globals.css (token values from plan 01)
  </read_first>
  <action>
**App.tsx changes** (D-11, D-12, D-23):

1. Remove the `BG` gradient mesh constant entirely. Replace with clean background:
```tsx
const BG = 'var(--color-bg)'
```
Or just inline `background: 'var(--color-bg)'` in the AppShell div style.

2. Nav pill active state — green (D-11, D-23). Update both button style objects:
```tsx
// Active state
background: isActive ? 'var(--color-brand)' : 'transparent',
color: isActive ? '#ffffff' : 'rgba(0,0,0,0.35)',
boxShadow: isActive ? '0 1px 4px rgba(37,180,57,0.30)' : 'none',
```

Where `!isMap` is the condition for Pulse button being active, and `isMap` for Map button.

3. Nav pill container — keep glass treatment (it floats over content, blur is appropriate).

**ContactPanel.tsx changes** (D-22):

Read the full file first. Then replace magic color values with token vars throughout:
- Panel background: if it uses `rgba(245,244,240,0.88)`, change to `var(--surface-panel)`
- Panel `backdropFilter`: if hardcoded, change to `var(--panel-blur)`
- Text primary colors: `rgba(0,0,0,0.82)` or `rgba(0,0,0,0.85)` → `var(--color-text-primary)`
- Text secondary: `rgba(0,0,0,0.45)` → `var(--color-text-secondary)`
- Text tertiary: `rgba(0,0,0,0.28)` → `var(--color-text-tertiary)`
- Panel title headings: add `fontFamily: 'var(--font-serif)'` per D-06

**ContactDetail.tsx changes** (D-22):

Read the full file first. Then:
- Contact name heading: add `fontFamily: 'var(--font-serif)'` (the contact's name is a heading)
- Section headings (Milestones, Interests, etc.): add `fontFamily: 'var(--font-serif)'` per D-06
- Replace magic text colors with token vars (same mapping as above)
- Leave `RING_COLORS` as-is — they're semantic interaction type colors, not design tokens
- SegmentedEquityRing: update the gradient to green-to-teal to match dashboard:
  - If it has its own gradient, change to green-to-teal: `#25B439` → `#00C9B1`
  - If it references the dashboard gradient, it's already updated

**ContactCard.tsx changes** (D-22):

Read the full file first. Replace magic color values:
- Name text: `rgba(0,0,0,0.82)` → `var(--color-text-primary)`
- Company/role text: `rgba(0,0,0,0.45)` → `var(--color-text-secondary)`
- Metadata text: `rgba(0,0,0,0.28)` → `var(--color-text-tertiary)`

**docs/design-system.md rewrite:**

Rewrite to reflect the Trolley-aligned design system. Key sections:

1. **Principles** — update from "glass and light" to "bold color and clarity"
2. **Color** — new token table:
   - Brand: `#25B439` (primary), `#1A8A2A` (dark)
   - Background: `#f8f8f6` (body), `#ffffff` (surface)
   - Hub orb: `#1C1C1E`
   - Text: primary `rgba(0,0,0,0.82)`, secondary `rgba(0,0,0,0.45)`, tertiary `rgba(0,0,0,0.28)`
   - On-brand text: `#ffffff`, `rgba(255,255,255,0.70)`, `rgba(255,255,255,0.55)`
3. **Typography** — Playfair Display 700/800 for headings, Plus Jakarta Sans 400/500/600 for body
4. **Orbs** — solid fills with subtle radial gradient for depth, dark hub, white labels, drop shadows
5. **Surfaces** — panel bg `rgba(255,255,255,0.92)`, blur, border
6. **Dashboard** — green header band, white text on green, pod cards with left color border
7. **Spacing** — 8pt grid (unchanged)
8. **Motion** — keep existing curves (unchanged)
9. **Accessibility** — updated contrast ratios for white-on-green text

Remove all glass orb rules (three-layer gradient, 0.16 opacity ceiling, glow system). Remove DM Sans references.
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/kinshipbrain && grep -c "var(--color-brand)" src/App.tsx && grep "rgba(180,160,255" src/App.tsx; echo "exit:$?" && grep -c "var(--font-serif)" src/components/contacts/ContactDetail.tsx && grep "Playfair Display" docs/design-system.md | head -1 && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx contains `var(--color-brand)` for nav active state
    - App.tsx does NOT contain `rgba(180,160,255` (old lavender gradient)
    - App.tsx does NOT contain `rgba(255,160,100` (old peach gradient)
    - App.tsx nav active button color is `#ffffff`
    - ContactPanel.tsx contains `var(--color-text-primary)` (at least 1 occurrence)
    - ContactPanel.tsx contains `var(--font-serif)` for heading
    - ContactDetail.tsx contains `var(--font-serif)` for contact name or section headings
    - ContactDetail.tsx contains `var(--color-text-primary)` (at least 1 occurrence)
    - ContactCard.tsx contains `var(--color-text-primary)` (at least 1 occurrence)
    - docs/design-system.md contains `Playfair Display`
    - docs/design-system.md contains `Plus Jakarta Sans`
    - docs/design-system.md contains `#25B439`
    - docs/design-system.md does NOT contain `DM Sans` (except in a "replaced" context)
    - docs/design-system.md does NOT contain `rgba(255,255,255,0.54)` (old glass base)
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>App background is clean near-white. Nav pill uses green active state with white text. Contact panels use new typography tokens and color tokens. Design system docs reflect the Trolley-aligned visual system. No glass-era references remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — demo readiness</name>
  <what-built>Complete Trolley CRM visual redesign: green brand color, Playfair Display headings, Plus Jakarta Sans body, solid orbs, green header band on dashboard, tokenized styles throughout, green nav pill.</what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open http://localhost:5173
    2. **Dashboard (/):**
       - Top section has green header band with white text
       - Equity ring uses white/white-alpha strokes on green (not orange-to-purple)
       - Score number and label are white on green
       - Stats block sits inside the green band with semi-transparent white background
       - Pod cards below green band have solid left borders in pod colors
       - "needs attention" and "today's focus" headings use serif font (Playfair Display)
       - Body text uses Plus Jakarta Sans (geometric, not DM Sans)
    3. **Nav pill:**
       - Active tab has green background with white text
       - Inactive tab has no background with dim text
    4. **Orb map (/map):**
       - Hub orb (MRM) is dark/near-black with white label
       - List orbs are solid colored (not glass with white base)
       - All orb labels are white text
       - Click a list orb — category orbs are also solid colored
       - Background is clean near-white (no lavender/peach/blue atmospheric gradient)
    5. **Contact panel:**
       - Click a category orb to open contact panel
       - Panel title uses serif font
       - Click a contact to open detail
       - Contact name heading uses serif font
       - Text colors are clean and consistent
    6. Compare overall feel against the Trolley CRM PDF — should feel like the same design language
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` passes clean — no TypeScript errors
- Dashboard green header band renders with white text on green
- Equity ring gradient is white on green (not orange-to-purple)
- Nav pill uses green active state
- Orbs are solid colored with white labels
- Background is clean near-white
- All headings use Playfair Display serif
- Body text uses Plus Jakarta Sans
- No `DM Sans` references remain
- No glass orb references remain (no `GlassOrb`, no three-layer gradient, no 0.16 opacity ceiling)
- design-system.md reflects the Trolley-aligned system
</verification>

<success_criteria>
The app looks polished enough to demo to Gwyneth. Green brand identity, editorial serif headings, solid orbs, green header band on dashboard, consistent tokenized styling. All 5 Trolley deltas implemented. VIS-02 and VIS-03 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-redesign/02-02-SUMMARY.md`
</output>
