---
phase: 18-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contexts/AuthContext.tsx
  - src/components/auth/RequireAuth.tsx
  - src/components/auth/SplashScreen.tsx
  - src/components/auth/LoginPage.tsx
  - src/main.tsx
  - src/App.tsx
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "Unauthenticated users are redirected to /login - no app content visible"
    - "Authenticated users see the app immediately - no login page flash"
    - "Demo mode bypasses auth entirely - unauthenticated users can explore with fake data"
    - "User session persists across browser refresh without re-authenticating"
    - "Login page renders outside AppShell - no nav, FAB, or demo toggle"
    - "After login, user returns to the URL they originally requested"
  artifacts:
    - path: "src/contexts/AuthContext.tsx"
      provides: "Auth state management via React context"
      exports: ["AuthProvider", "useAuth"]
    - path: "src/components/auth/RequireAuth.tsx"
      provides: "Route guard with demo mode bypass"
      exports: ["RequireAuth"]
    - path: "src/components/auth/SplashScreen.tsx"
      provides: "Branded loading screen during session check"
      exports: ["SplashScreen"]
    - path: "src/components/auth/LoginPage.tsx"
      provides: "Login page shell (Lovable UI dropped in later)"
      exports: ["LoginPage"]
  key_links:
    - from: "src/main.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "AuthProvider wraps App inside BrowserRouter"
      pattern: "<AuthProvider>"
    - from: "src/App.tsx"
      to: "src/components/auth/RequireAuth.tsx"
      via: "Route element wrapper around AppShell"
      pattern: "<Route element=.*RequireAuth"
    - from: "src/components/auth/RequireAuth.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuth() hook for session and loading state"
      pattern: "useAuth\\(\\)"
    - from: "src/components/auth/RequireAuth.tsx"
      to: "src/lib/sampleData.ts"
      via: "isDemoMode() check before auth guard"
      pattern: "isDemoMode\\(\\)"
---

<objective>
Wire Supabase authentication into the React app - auth context provider, route guard, splash screen, login page shell, and route restructuring.

Purpose: Gate all app content behind authentication (AUTH-02) while preserving demo mode access (D-06/D-07) and session persistence (AUTH-03). Auth UI components are built separately in Lovable (D-05) - this plan creates the routing and state management shell only.

Output: AuthProvider context, RequireAuth guard, SplashScreen, LoginPage shell, wired routes in App.tsx/main.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-authentication/18-CONTEXT.md
@.planning/phases/18-authentication/18-RESEARCH.md
@src/App.tsx
@src/main.tsx
@src/integrations/supabase/client.ts
@src/lib/sampleData.ts

<interfaces>
<!-- Existing exports the executor needs -->

From src/integrations/supabase/client.ts:
```typescript
export const supabase = createClient<Database>(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, {
  auth: { storage: localStorage, persistSession: true, autoRefreshToken: true }
})
```

From src/lib/sampleData.ts:
```typescript
export function isDemoMode(): boolean
export function setDemoMode(on: boolean): void
```

From src/main.tsx (current provider order):
```typescript
<StrictMode>
  <ReactFlowProvider>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </ReactFlowProvider>
</StrictMode>
```

From src/App.tsx (current route structure):
```typescript
<Routes>
  <Route element={<AppShell />}>
    <Route index element={<Dashboard />} />
    {/* ... all routes ... */}
  </Route>
</Routes>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthContext provider and useAuth hook</name>
  <files>src/contexts/AuthContext.tsx</files>
  <read_first>
    - src/integrations/supabase/client.ts (supabase client instance and auth config)
    - src/lib/sampleData.ts (isDemoMode export signature)
  </read_first>
  <action>
Create `src/contexts/AuthContext.tsx` with:

1. **AuthContextValue type:** `{ session: Session | null; loading: boolean }`
2. **AuthContext:** `createContext<AuthContextValue>({ session: null, loading: true })`
3. **AuthProvider component:**
   - State: `session` (Session | null, init null), `loading` (boolean, init true)
   - Single `useEffect` subscribing to `supabase.auth.onAuthStateChange` (per D-11)
   - Callback sets `session` from event and sets `loading = false`
   - Cleanup calls `subscription.unsubscribe()`
   - Do NOT call `supabase.auth.getSession()` separately - the `INITIAL_SESSION` event from `onAuthStateChange` delivers the persisted session on mount (per research anti-pattern)
   - Renders `<AuthContext.Provider value={{ session, loading }}>{children}</AuthContext.Provider>`
4. **useAuth hook:** `export const useAuth = () => useContext(AuthContext)`

Import `Session` type from `@supabase/supabase-js`. Import `supabase` from `@/integrations/supabase/client`.
  </action>
  <verify>
    <automated>grep -c "onAuthStateChange" src/contexts/AuthContext.tsx && grep -c "export function AuthProvider" src/contexts/AuthContext.tsx && grep -c "export const useAuth" src/contexts/AuthContext.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/contexts/AuthContext.tsx exists
    - File contains `export function AuthProvider`
    - File contains `export const useAuth`
    - File contains `onAuthStateChange` (exactly one subscription)
    - File contains `Session` import from `@supabase/supabase-js`
    - File contains `supabase` import from `@/integrations/supabase/client`
    - File does NOT contain `getSession` (anti-pattern per research)
    - File contains `loading: true` as initial state
    - File contains `subscription.unsubscribe()` in cleanup
  </acceptance_criteria>
  <done>AuthProvider manages session state via onAuthStateChange, useAuth hook exported, no getSession anti-pattern</done>
</task>

<task type="auto">
  <name>Task 2: Create RequireAuth guard, SplashScreen, and LoginPage shell</name>
  <files>src/components/auth/RequireAuth.tsx, src/components/auth/SplashScreen.tsx, src/components/auth/LoginPage.tsx</files>
  <read_first>
    - src/contexts/AuthContext.tsx (useAuth hook just created in Task 1)
    - src/lib/sampleData.ts (isDemoMode function signature)
    - src/App.tsx (current AppShell layout pattern, design token usage like BG var)
    - docs/design-system.md (color tokens, typography for login page per D-04)
  </read_first>
  <action>
**SplashScreen.tsx** (per D-08, D-09):
- Full viewport centered layout: `width: 100vw, height: 100vh, display: flex, alignItems: center, justifyContent: center`
- Background: `var(--color-bg)` (same as app background, #F5F4F0)
- Centered content: "RealDeal" text in `var(--font-serif)` (Fraunces), `fontSize: 28`, `fontWeight: 700`, `color: rgba(0,0,0,0.82)`
- No spinner, no animation - just the branded wordmark. Clean and intentional.
- Export: `export function SplashScreen()`

**RequireAuth.tsx** (per D-01, D-02, D-06, D-07):
- Import `useAuth` from `@/contexts/AuthContext`, `isDemoMode` from `@/lib/sampleData`, `Navigate` and `useLocation` and `Outlet` from `react-router`, `SplashScreen` from `./SplashScreen`
- Check order (critical - per research Pattern 2):
  1. If `isDemoMode()` returns true, render `<Outlet />` immediately - skip all auth checks (D-07)
  2. If `loading` is true, render `<SplashScreen />` (D-08 - prevents login flash for authenticated users)
  3. If no `session`, render `<Navigate to={'/login?return_to=' + encodeURIComponent(location.pathname + location.search)} replace />` (D-01 return-to pattern)
  4. Otherwise render `<Outlet />`
- Export: `export function RequireAuth()`

**LoginPage.tsx** (per D-03, D-04, D-05):
- Full viewport centered layout (same structure as SplashScreen)
- Background: `var(--color-bg)`, font: `var(--font-sans)` (DM Sans)
- Import `useAuth` from `@/contexts/AuthContext`, `useSearchParams` and `useNavigate` from `react-router`
- `useEffect`: if `session` exists, navigate to `searchParams.get('return_to') ?? '/'` with `{ replace: true }` (D-01). Validate `return_to` starts with `/` to prevent open redirect - ignore any value that doesn't start with `/`.
- Render a centered container with:
  - "RealDeal" heading in `var(--font-serif)`, `fontSize: 32`, `fontWeight: 700`
  - Subtitle text: "Sign in to continue" in `fontSize: 14`, `color: rgba(0,0,0,0.45)`, `marginTop: 8`
  - A placeholder `div` with `id="lovable-auth"` styled as a centered column (`maxWidth: 360`, `width: 100%`, `marginTop: 32`) where Lovable auth UI will be mounted (D-05)
- This is a shell only - no auth form, no OAuth buttons. Lovable provides those.
- Export: `export function LoginPage()`
  </action>
  <verify>
    <automated>ls src/components/auth/RequireAuth.tsx src/components/auth/SplashScreen.tsx src/components/auth/LoginPage.tsx && grep -c "isDemoMode()" src/components/auth/RequireAuth.tsx && grep -c "return_to" src/components/auth/LoginPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/auth/RequireAuth.tsx exists and contains `isDemoMode()` check BEFORE loading check
    - RequireAuth.tsx contains `<Navigate to=` with `return_to` query param and `replace`
    - RequireAuth.tsx contains `<SplashScreen />` render when loading
    - RequireAuth.tsx contains `<Outlet />` render (used twice: demo bypass and authenticated)
    - src/components/auth/SplashScreen.tsx exists and contains `var(--color-bg)` and `var(--font-serif)`
    - src/components/auth/LoginPage.tsx exists and contains `return_to` handling
    - LoginPage.tsx contains open redirect guard: `startsWith('/')` check on return_to value
    - LoginPage.tsx contains `var(--font-serif)` for heading and `var(--color-bg)` for background
    - LoginPage.tsx contains `replace: true` in navigate call
    - LoginPage.tsx does NOT contain any auth form elements or OAuth buttons (D-05)
  </acceptance_criteria>
  <done>RequireAuth guards routes with demo bypass first, SplashScreen shows branded loading, LoginPage is a shell ready for Lovable auth UI</done>
</task>

<task type="auto">
  <name>Task 3: Wire AuthProvider into main.tsx and restructure App.tsx routes</name>
  <files>src/main.tsx, src/App.tsx</files>
  <read_first>
    - src/main.tsx (current provider nesting order)
    - src/App.tsx (current route structure, all imports, AppShell component)
    - src/contexts/AuthContext.tsx (AuthProvider export)
    - src/components/auth/RequireAuth.tsx (RequireAuth export)
    - src/components/auth/LoginPage.tsx (LoginPage export)
  </read_first>
  <action>
**main.tsx** (per research Pattern 5):
- Add import: `import { AuthProvider } from './contexts/AuthContext'`
- Wrap `<App />` with `<AuthProvider>` INSIDE `<BrowserRouter>` but OUTSIDE `<App />`:
```
<StrictMode>
  <ReactFlowProvider>
    <BrowserRouter>
      <AuthProvider>
        <App />
      </AuthProvider>
    </BrowserRouter>
  </ReactFlowProvider>
</StrictMode>
```
- Keep all existing imports and structure unchanged

**App.tsx** (per research Pattern 3):
- Add imports at top:
  - `import { RequireAuth } from './components/auth/RequireAuth'`
  - `import { LoginPage } from './components/auth/LoginPage'`
- Restructure the `<Routes>` block in the `App` component:
```tsx
<Routes>
  <Route path="login" element={<LoginPage />} />
  <Route element={<RequireAuth />}>
    <Route element={<AppShell />}>
      <Route index element={<Dashboard />} />
      <Route path="pulse/nurturing" element={<NurturingHub />} />
      <Route path="map" element={<OrbMap />} />
      <Route path="contacts" element={<RecordsList />} />
      <Route path="pipelines" element={<PipelinesPage />} />
      <Route path="projects" element={<ProjectsPage />} />
      <Route path="projects/:id" element={<ProjectDetailPage />} />
      <Route path="category/:id" element={<CategoryTable />} />
      <Route path="contact/:id" element={<RecordPage />} />
      <Route path="pod/:id" element={<PodDetailPage />} />
      <Route path="import" element={<ImportPanel />} />
    </Route>
  </Route>
</Routes>
```
- Key structure: `/login` route is OUTSIDE `RequireAuth` (D-03). `RequireAuth` wraps `AppShell` and renders `<Outlet />`, so `AppShell` nests inside it.
- Do NOT modify `AppShell` function - it remains unchanged. The demo toggle stays inside AppShell (accessible once auth or demo mode passes the guard).
- Do NOT remove any existing imports or routes.
  </action>
  <verify>
    <automated>grep -c "AuthProvider" src/main.tsx && grep "RequireAuth" src/App.tsx && grep "LoginPage" src/App.tsx && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/main.tsx contains `<AuthProvider>` wrapping `<App />`
    - src/main.tsx has `AuthProvider` INSIDE `BrowserRouter` (grep shows BrowserRouter before AuthProvider in render tree)
    - src/App.tsx contains `import { RequireAuth }` from auth directory
    - src/App.tsx contains `import { LoginPage }` from auth directory
    - src/App.tsx route structure: `<Route path="login"` appears BEFORE `<Route element={<RequireAuth`
    - src/App.tsx has `<Route element={<RequireAuth />}>` wrapping `<Route element={<AppShell />}>`
    - All existing routes (index, pulse/nurturing, map, contacts, pipelines, projects, etc.) remain present inside AppShell
    - `pnpm build` exits with code 0 (type-check passes)
  </acceptance_criteria>
  <done>AuthProvider wraps app in main.tsx, login route sits outside RequireAuth, all app routes protected behind RequireAuth > AppShell nesting, build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete auth wiring: AuthProvider context, RequireAuth route guard with demo bypass, SplashScreen, LoginPage shell, restructured routes</what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open http://localhost:5173
    2. With demo mode OFF: you should be redirected to /login - verify no app content (nav, FAB, dashboard) is visible
    3. The login page should show "RealDeal" heading and "Sign in to continue" subtitle on the cream background - no auth form yet (Lovable handles that)
    4. Navigate to http://localhost:5173/map directly - should redirect to /login?return_to=%2Fmap
    5. Toggle demo mode: visit /login, open browser console, run `localStorage.setItem('realdeal:demo', 'true')` then reload http://localhost:5173 - you should see the full app with demo data, no login required
    6. Turn demo off: console `localStorage.removeItem('realdeal:demo')` and reload - should redirect back to /login
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` passes with zero type errors
- `/login` route renders LoginPage outside AppShell
- All other routes redirect to `/login` when not authenticated and not in demo mode
- Demo mode bypasses auth guard completely
- No `getSession()` call in AuthContext (only `onAuthStateChange`)
- `return_to` parameter preserved through login redirect cycle
</verification>

<success_criteria>
- AUTH-01: LoginPage shell exists ready for Lovable auth UI (sign up/login wiring is Lovable's job)
- AUTH-02: Unauthenticated non-demo users see only the login page
- AUTH-03: Session persists via Supabase client config + onAuthStateChange rehydration on mount
- Demo mode bypasses auth entirely per D-06/D-07
- No login page flash for authenticated users per D-08/D-09
- Build passes, no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-authentication/18-01-SUMMARY.md`
</output>
