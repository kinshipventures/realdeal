---
phase: 02.1-design-implementation
plan: 06
type: execute
wave: 3
depends_on: ["02.1-01"]
files_modified:
  - src/components/empty/EmptyState.tsx
  - src/components/dashboard/Dashboard.tsx
  - src/components/contacts/ContactPanel.tsx
  - src/components/contacts/InteractionSection.tsx
  - src/components/map/OrbMap.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Empty map shows 'Your network starts here' with orb illustration and 'Create first pod' CTA"
    - "Empty dashboard shows 'No pulse yet' with orb illustration and 'Log interaction' CTA"
    - "Empty pod shows '{Pod name} is empty' with 'Add contact' CTA"
    - "Empty interactions shows 'No history yet' with 'Log first interaction' CTA"
  artifacts:
    - path: "src/components/empty/EmptyState.tsx"
      provides: "Reusable empty state component with orb illustration and CTA"
      contains: "EmptyState"
    - path: "src/components/dashboard/Dashboard.tsx"
      provides: "Uses EmptyState for no-activity state"
      contains: "No pulse yet"
    - path: "src/components/contacts/InteractionSection.tsx"
      provides: "Uses EmptyState for no-interactions state"
      contains: "No history yet"
  key_links:
    - from: "src/components/empty/EmptyState.tsx"
      to: "src/components/dashboard/Dashboard.tsx"
      via: "EmptyState import"
      pattern: "import.*EmptyState"
    - from: "src/components/empty/EmptyState.tsx"
      to: "src/components/contacts/InteractionSection.tsx"
      via: "EmptyState import"
      pattern: "import.*EmptyState"
---

<objective>
Empty states with orb illustrations, ghost hint cards, warm copy, and one primary CTA per state.

Purpose: Every zero-data state needs intentional design — not "No data found." The DESIGN.md specifies 6 empty states, each with a colored orb illustration, ghost hints showing the shape of future data, and one clear action. These make an empty app feel inviting rather than broken.

Output: New EmptyState component. Integrated into Dashboard, ContactPanel, OrbMap, and InteractionSection.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EmptyState component</name>
  <files>src/components/empty/EmptyState.tsx</files>
  <read_first>
    - DESIGN.md (Empty States section — all 6 states, copy, CTA buttons)
    - docs/design-exploration/09-empty-states.html (all 6 mockups — orb illustration CSS, ghost card CSS, CTA button)
  </read_first>
  <action>
    Create `src/components/empty/EmptyState.tsx` — a reusable component for all empty states.

    Props interface:
    ```typescript
    interface EmptyStateProps {
      icon: React.ReactNode  // Lucide-style SVG icon to render inside the orb
      heading: string        // e.g. "Your network starts here"
      subtext?: string       // optional description below heading
      ctaLabel?: string      // e.g. "Create first pod"
      onCta?: () => void     // CTA click handler
      orbColor?: string      // orb illustration color, defaults to var(--color-brand)
      ghosts?: number        // number of ghost hint cards to show (0-3)
    }
    ```

    Layout (centered, vertical stack):
    ```
    [Orb illustration with icon inside]
    [Heading — Fraunces 700, 15px]
    [Subtext — 12px, secondary color]
    [Ghost hint cards — dashed border placeholders]
    [CTA button — green, 10px radius]
    ```

    Orb illustration:
    - 56px circle, background color at 12% opacity: `background: ${orbColor}14` (hex notation)
    - Icon inside: 28px, color matching orbColor
    - Subtle glow: `boxShadow: \`0 0 20px ${orbColor}20\``

    Ghost hint cards (if ghosts > 0):
    - Render `ghosts` number of placeholder rows
    - Each: height 44px, border: `1px dashed rgba(0,0,0,0.10)`, borderRadius 10px
    - Gap between ghosts: 8px
    - Margin: 16px 0

    CTA button:
    - Background: var(--color-brand), color: white
    - Padding: 10px 20px, borderRadius: 10px
    - Font: var(--font-sans), 13px, weight 600
    - Hover: var(--color-brand-dark)

    Overall container:
    - Display: flex, flexDirection: column, alignItems: center, justifyContent: center
    - Gap: 12px
    - Padding: 40px 20px
    - minHeight: 200px
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/kinshipbrain && test -f src/components/empty/EmptyState.tsx && grep "EmptyState" src/components/empty/EmptyState.tsx && grep "ghost" src/components/empty/EmptyState.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/empty/EmptyState.tsx exists
    - EmptyState accepts icon, heading, ctaLabel, onCta, orbColor, ghosts props
    - EmptyState renders a 56px orb illustration circle with icon inside
    - EmptyState renders ghost hint cards with `dashed` border when ghosts > 0
    - EmptyState renders green CTA button when ctaLabel and onCta provided
    - Heading uses `var(--font-serif)` at 700 weight
  </acceptance_criteria>
  <done>Reusable EmptyState component created with orb illustration, heading, ghost hints, and CTA button</done>
</task>

<task type="auto">
  <name>Task 2: Integrate empty states into Dashboard, ContactPanel, OrbMap, and InteractionSection</name>
  <files>src/components/dashboard/Dashboard.tsx, src/components/contacts/ContactPanel.tsx, src/components/contacts/InteractionSection.tsx, src/components/map/OrbMap.tsx</files>
  <read_first>
    - src/components/empty/EmptyState.tsx (just created — import path and props)
    - src/components/dashboard/Dashboard.tsx (find where empty/loading states are currently handled)
    - src/components/contacts/ContactPanel.tsx (find where empty contact list is rendered)
    - src/components/contacts/InteractionSection.tsx (find where empty interactions list is rendered — look for empty array check or "no interactions" text)
    - src/components/map/OrbMap.tsx (find where empty pod list is handled)
    - DESIGN.md (Empty States table — exact copy for each state)
  </read_first>
  <action>
    1. Dashboard.tsx — when no pods or no interactions loaded and not loading:
       - Import EmptyState from '../empty/EmptyState'
       - Render: `<EmptyState heading="No pulse yet" ctaLabel="Log interaction" onCta={...} ghosts={2} icon={/* Activity icon SVG */} />`
       - The Activity icon (Lucide): `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>`

    2. ContactPanel.tsx — when contacts array is empty and not loading:
       - Import EmptyState
       - Render: `<EmptyState heading="No one here yet" ctaLabel="Add contact" onCta={() => setShowNewContact(true)} ghosts={3} icon={/* Users icon SVG */} />`
       - Users icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>`

    3. InteractionSection.tsx — when interactions array is empty and not loading:
       - Import EmptyState from '../empty/EmptyState'
       - Find the existing empty check (where interactions.length === 0 or similar)
       - Render: `<EmptyState heading="No history yet" ctaLabel="Log first interaction" onCta={() => { /* focus/open the interaction log form */ }} ghosts={2} icon={/* Clock icon SVG */} />`
       - Clock icon (Lucide): `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>`

    4. OrbMap.tsx — when pods array is empty and not loading:
       - Import EmptyState
       - Render centered on the canvas: `<EmptyState heading="Your network starts here" ctaLabel="Create first pod" ghosts={0} icon={/* Globe icon SVG */} />`
       - This should render as an overlay on the React Flow canvas
       - Globe icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>`

    5. "All caught up" state — in Dashboard overdue/focus section, when no overdue contacts:
       - Render: `<EmptyState heading="All caught up" subtext="Nothing needs attention right now" icon={/* Check icon */} />`
       - No CTA needed for this one (omit ctaLabel)
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/kinshipbrain && grep "EmptyState" src/components/dashboard/Dashboard.tsx && grep "No pulse yet" src/components/dashboard/Dashboard.tsx && grep "EmptyState" src/components/contacts/ContactPanel.tsx && grep "No one here yet" src/components/contacts/ContactPanel.tsx && grep "EmptyState" src/components/contacts/InteractionSection.tsx && grep "No history yet" src/components/contacts/InteractionSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.tsx imports and uses EmptyState with heading "No pulse yet"
    - ContactPanel.tsx imports and uses EmptyState with heading "No one here yet"
    - InteractionSection.tsx imports and uses EmptyState with heading "No history yet"
    - OrbMap.tsx imports and uses EmptyState with heading "Your network starts here"
    - Dashboard.tsx shows "All caught up" when no overdue contacts
    - All empty states use Lucide-style inline SVG icons (28px, 1.5px stroke)
    - `pnpm build` passes
  </acceptance_criteria>
  <done>Empty states integrated — Dashboard shows "No pulse yet", ContactPanel shows "No one here yet", InteractionSection shows "No history yet", OrbMap shows "Your network starts here", overdue section shows "All caught up"</done>
</task>

</tasks>

<verification>
- `pnpm build` passes
- Each empty state renders with orb illustration, correct heading, and CTA button
- Ghost hint cards appear as dashed-border placeholders
- Empty states use warm, human copy (not "No data found")
- InteractionSection empty state renders "No history yet" when interactions array is empty
</verification>

<success_criteria>
- EmptyState component is reusable with icon, heading, ghosts, CTA props
- All 5 empty states integrated (map, dashboard, pod, interactions, all-caught-up)
- Orb illustration: 56px circle at 12% pod color opacity with icon inside
- Ghost hints: dashed border placeholder rows
- CTA: green button with 10px radius
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-design-implementation/02.1-06-SUMMARY.md`
</output>
