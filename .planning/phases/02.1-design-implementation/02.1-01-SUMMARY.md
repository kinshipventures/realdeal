---
phase: 02.1-design-implementation
plan: 01
subsystem: ui
tags: [css, fonts, dark-mode, accessibility, typography, design-tokens]

requires: []
provides:
  - Fraunces serif font loaded via Google Fonts (index.html + globals.css @import)
  - Dark mode CSS token overrides via prefers-color-scheme media query
  - Interaction type color tokens (--interaction-call/email/text/meeting/note/intro)
  - Focus ring (*:focus-visible, 2px solid brand green, 2px offset)
  - Hub orb dark mode border (.moj-orb)
  - prefers-reduced-motion disables orb/panel animations
  - dormancyLabel() export from equity.ts
  - RING_COLORS updated to DESIGN.md semantic palette
affects:
  - All subsequent 02.1 plans depend on Fraunces being loaded
  - Dark mode token overrides cascade to all components
  - Interaction color tokens available for 02.1-03 (orb system)

tech-stack:
  added: [Fraunces (Google Fonts variable serif)]
  patterns:
    - Dark mode via prefers-color-scheme with @theme + :root dual overrides
    - Interaction semantic color tokens as CSS custom properties

key-files:
  created: []
  modified:
    - src/styles/globals.css
    - index.html
    - src/lib/equity.ts
    - src/components/contacts/ContactDetail.tsx

key-decisions:
  - "Fraunces replaces Playfair Display — heavier weight matches Trolley PDF confidence"
  - "Dark mode via prefers-color-scheme only (no manual toggle for v1)"
  - "RING_COLORS aligned to DESIGN.md semantic palette (intro=pink, call=forest green, email=blue, etc.)"

patterns-established:
  - "CSS token dual override: @theme block for Tailwind utility mapping, :root block for component constants"

requirements-completed: []

duration: 12min
completed: 2026-03-23
---

# Phase 02.1 Plan 01: Foundation Tokens Summary

**Fraunces font swap, dark mode CSS token overrides, interaction color tokens, focus ring, and dormancy label copy — foundation for all subsequent design plans**

## Performance

- **Duration:** 12 min
- **Started:** 2026-03-23T~17:50:00Z
- **Completed:** 2026-03-23T~18:02:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Fraunces variable serif loaded via Google Fonts in both index.html and globals.css @import
- Dark mode fully wired via prefers-color-scheme — bg, surfaces, text hierarchy, orb shadows, brand green all flip
- Six interaction type CSS tokens added to :root for downstream use in timeline + ring components
- dormancyLabel() function exported from equity.ts (Slipping away / Going quiet / Cooling off)
- RING_COLORS in ContactDetail updated to DESIGN.md semantic palette
- focus-visible ring, reduced-motion support, and hub orb dark border all added

## Task Commits

1. **Task 1: Fraunces font swap + dark mode tokens + interaction colors + focus ring** - `0782868` (feat)
2. **Task 2: Copy and voice updates — score labels, field renames, placeholder copy** - `464eec2` (feat)

## Files Created/Modified
- `src/styles/globals.css` - Fraunces import, dark mode @theme + :root, interaction tokens, focus ring, reduced-motion
- `index.html` - Updated Google Fonts link to Fraunces
- `src/lib/equity.ts` - Added dormancyLabel() export
- `src/components/contacts/ContactDetail.tsx` - Updated RING_COLORS to DESIGN.md palette

## Decisions Made
- Field labels (Focus, Known for, Intro'd by, Context) and score labels (Thriving/Steady/Cooling/Fading) were already correct in the existing code — no changes needed there
- InteractionSection `What happened?` placeholder was already in place — no change needed

## Deviations from Plan

None - plan executed exactly as written. Minor note: several "copy updates" listed in the plan were already implemented from a previous phase — confirmed by reading source files before making changes.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Fraunces now loaded; all serif headings will render with correct weight
- Dark mode tokens in place for 02.1-02 (orb system) to use
- Interaction color tokens available for timeline + ring work in 02.1-03
- No blockers for wave 1 continuation

---
*Phase: 02.1-design-implementation*
*Completed: 2026-03-23*
