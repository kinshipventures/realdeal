---
phase: 02.1-design-implementation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/map/SolidOrb.tsx
  - src/components/map/MojNode.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Orbs display a two-tone hue-shifted diagonal gradient (not flat fill)"
    - "Orbs have a colored glow halo that intensifies on hover"
    - "Orbs have an SVG health ring showing equity score as arc fill"
    - "Hub orb has two-tone dark gradient with no hover scale"
  artifacts:
    - path: "src/components/map/SolidOrb.tsx"
      provides: "Two-tone gradient orb with glow halo and health ring"
      contains: "linear-gradient(135deg"
    - path: "src/components/map/MojNode.tsx"
      provides: "Hub orb with dark two-tone gradient"
      contains: "linear-gradient(135deg, #1C1C1E"
  key_links:
    - from: "src/components/map/SolidOrb.tsx"
      to: "src/components/map/ListNode.tsx"
      via: "SolidOrb import"
      pattern: "import.*SolidOrb"
---

<objective>
Rewrite the orb component from flat solid fill to two-tone hue-shifted gradient with glow halos and optional health ring SVG.

Purpose: Two-tone color shift is the locked orb surface decision. Each orb needs internal color motion (diagonal gradient from base to hue-shifted neighbor), a colored ambient glow via box-shadow, and a thin SVG health ring arc showing equity score. The glow intensifies on hover.

Output: Rewritten SolidOrb.tsx with two-tone gradient, glow halo, health ring. Updated MojNode with dark two-tone gradient.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@DESIGN.md

<interfaces>
From src/components/map/SolidOrb.tsx (current interface to preserve):
```typescript
interface SolidOrbProps {
  size: number
  color: HexColor
  glowIntensity?: 'low' | 'high'
  animationDelay?: string
  onClick?: () => void
  className?: string
  children: React.ReactNode
}
```

From src/components/map/ListNode.tsx (consumer — uses SolidOrb):
```typescript
<SolidOrb size={SIZE} color={color} glowIntensity={...} animationDelay={...} onClick={...}>
```

From src/components/map/CategoryNode.tsx (consumer):
```typescript
<SolidOrb size={60} color={color} animationDelay={...} onClick={...}>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SolidOrb with two-tone gradient, glow halo, and health ring</name>
  <files>src/components/map/SolidOrb.tsx</files>
  <read_first>
    - src/components/map/SolidOrb.tsx (current implementation, props interface)
    - src/components/map/ListNode.tsx (how SolidOrb is consumed — props passed)
    - src/components/map/CategoryNode.tsx (how SolidOrb is consumed)
    - docs/design-exploration/04-final-system.html (lines 43-55, 172-200 — orb CSS: gradient, glow box-shadow, health ring SVG)
    - docs/design-exploration/03-orb-surface-map-concepts.html (lines 74-82 — two-tone CSS pattern)
    - DESIGN.md (Orb System section — colors, sizes, glow values, health ring spec)
  </read_first>
  <action>
    Rewrite SolidOrb.tsx. Expand the props interface to add:
    - `shiftColor?: HexColor` — the hue-shifted neighbor for the gradient endpoint
    - `healthPercent?: number` — 0-100, drives the health ring arc fill
    - Keep existing props (size, color, glowIntensity, animationDelay, onClick, className, children)

    Two-tone gradient surface:
    - `background: linear-gradient(135deg, ${color} 0%, ${shiftColor ?? color} 100%)`
    - If no shiftColor provided, falls back to single color (backward compatible)

    Glow halo via box-shadow (replace current orb-shadow-base):
    - At rest: `0 0 ${glowSize}px ${hexToRgba(color, 0.25)}, 0 4px 16px rgba(0,0,0,0.12)`
    - Glow size: size >= 96 ? 28 : 20 (larger orbs get bigger glow)
    - On hover: opacity increases to 0.40 — use CSS custom property `--orb-glow` set inline
    - Override the `.orb-interactive` box-shadow with inline `--orb-glow-rest` and `--orb-glow-hover` CSS vars

    Update CSS in globals.css (or inline styles):
    - Set `style={{ '--orb-glow-color': hexToRgba(color, 0.25) }}` and use in .orb-interactive

    Actually, simplest approach: set box-shadow directly on the div since glow is color-dependent:
    - `boxShadow: \`0 0 ${glowSize}px ${hexToRgba(color, 0.25)}, 0 4px 16px rgba(0,0,0,0.12)\``
    - Add `onMouseEnter` / `onMouseLeave` to swap glow opacity between 0.25 and 0.40
    - Store hover state in a ref (not useState) to avoid re-renders — update boxShadow via ref

    Health ring SVG (optional, rendered when healthPercent is provided):
    - Wrap the orb body in a container div
    - Position an SVG absolutely behind the orb body, centered
    - SVG viewBox matches container size (size + 8px for 4px gap on each side)
    - Track circle: `stroke="rgba(0,0,0,0.06)"` `stroke-width="2"` full circle
    - Fill circle: `stroke={color}` `stroke-width="2"` `stroke-dasharray` based on healthPercent
    - Arc calculation: `circumference = 2 * Math.PI * radius`, `dasharray = \`${(healthPercent/100) * circumference} ${circumference}\``
    - `stroke-linecap="round"` `transform="rotate(-90 cx cy)"` to start from top
    - The ring radius is `(containerSize - 2) / 2` so the stroke sits 4px from orb edge

    Hover states (keep existing behavior, add glow):
    - Hover: scale up (1.05 for 96px+, 1.08 for smaller) + translateY(-3px) + glow to 0.40
    - Press: scale 0.94, 0.08s ease-in (already handled by .orb-interactive:active in CSS)
    - Cursor: pointer for clickable orbs

    Keep orb-enter and orb-interactive CSS classes.

    Pod color map for shiftColor — consumers (ListNode, CategoryNode) can pass shiftColor, but SolidOrb should work without it. The color mapping from DESIGN.md:
    - Maps: #E53935 → #FF6B4A
    - LPs: #FF6B8A → #FF8FA5
    - Companies: #7E57C2 → #5C6BC0
    - Talent: #25B439 → #00BFA5
    - Service Providers: #F5A623 → #FFD54F
    - Hub: #1C1C1E → #2C2C30

    Export a `POD_SHIFT_COLORS` map from SolidOrb.tsx so consumers can look up shift colors:
    ```typescript
    export const POD_SHIFT_COLORS: Record<string, string> = {
      '#E53935': '#FF6B4A',
      '#FF6B8A': '#FF8FA5',
      '#7E57C2': '#5C6BC0',
      '#25B439': '#00BFA5',
      '#F5A623': '#FFD54F',
      '#1C1C1E': '#2C2C30',
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/kinshipbrain && grep "linear-gradient(135deg" src/components/map/SolidOrb.tsx && grep "healthPercent" src/components/map/SolidOrb.tsx && grep "POD_SHIFT_COLORS" src/components/map/SolidOrb.tsx && grep "0.25" src/components/map/SolidOrb.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SolidOrb.tsx contains `linear-gradient(135deg, ${color}` (two-tone gradient)
    - SolidOrb.tsx exports `POD_SHIFT_COLORS` record
    - SolidOrb.tsx accepts `shiftColor?: HexColor` prop
    - SolidOrb.tsx accepts `healthPercent?: number` prop
    - SolidOrb.tsx renders an SVG with `stroke-dasharray` when healthPercent is provided
    - SolidOrb.tsx has glow box-shadow using pod color at 0.25 opacity
    - SolidOrb.tsx has hover handler that changes glow to 0.40 opacity
    - Existing props (size, color, onClick, children, className, animationDelay) still work
  </acceptance_criteria>
  <done>SolidOrb renders two-tone gradient surface, colored glow halo that intensifies on hover, and optional health ring SVG arc. Backward compatible with all existing consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Update MojNode and ListNode to use two-tone gradient + shift colors</name>
  <files>src/components/map/MojNode.tsx, src/components/map/ListNode.tsx, src/components/map/CategoryNode.tsx</files>
  <read_first>
    - src/components/map/MojNode.tsx (current hub orb — flat #1C1C1E)
    - src/components/map/ListNode.tsx (current list node — passes color to SolidOrb)
    - src/components/map/CategoryNode.tsx (current category node)
    - src/components/map/SolidOrb.tsx (just-updated with shiftColor, healthPercent, POD_SHIFT_COLORS)
  </read_first>
  <action>
    1. MojNode.tsx — update hub orb to use two-tone gradient:
       - Change `background: '#1C1C1E'` to `background: 'linear-gradient(135deg, #1C1C1E 0%, #2C2C30 100%)'`
       - Add subtle glow: `boxShadow: '0 0 24px rgba(0,0,0,0.20), 0 6px 24px rgba(0,0,0,0.25)'`
       - No hover scale (hub is not interactive). Remove orb-interactive class if present, keep orb-enter.
       - No health ring on hub orb.

    2. ListNode.tsx — pass shiftColor to SolidOrb:
       - Import `POD_SHIFT_COLORS` from './SolidOrb'
       - Look up shift color: `const shiftColor = POD_SHIFT_COLORS[color.toUpperCase()] ?? POD_SHIFT_COLORS[color] as HexColor | undefined`
       - Pass `shiftColor={shiftColor as HexColor}` to SolidOrb
       - Health ring: the ListNode has access to contactCount and overdueCount but not equity score yet. Leave healthPercent undefined for now — Plan 03 (map) will wire equity data through.

    3. CategoryNode.tsx — pass shiftColor to SolidOrb:
       - Import `POD_SHIFT_COLORS` from './SolidOrb'
       - Same lookup pattern
       - Pass shiftColor to SolidOrb
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/kinshipbrain && grep "linear-gradient(135deg, #1C1C1E" src/components/map/MojNode.tsx && grep "POD_SHIFT_COLORS" src/components/map/ListNode.tsx && grep "shiftColor" src/components/map/ListNode.tsx</automated>
  </verify>
  <acceptance_criteria>
    - MojNode.tsx contains `linear-gradient(135deg, #1C1C1E 0%, #2C2C30 100%)`
    - MojNode.tsx has hub glow box-shadow with `rgba(0,0,0,0.20)`
    - ListNode.tsx imports POD_SHIFT_COLORS from SolidOrb
    - ListNode.tsx passes shiftColor prop to SolidOrb
    - CategoryNode.tsx imports POD_SHIFT_COLORS and passes shiftColor
    - `pnpm build` passes with no type errors
  </acceptance_criteria>
  <done>All orb nodes render with two-tone gradient. Hub has dark gradient. Pod and category orbs look up their shift color from the POD_SHIFT_COLORS map.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes
- Orb map shows two-tone gradient orbs with colored glow halos
- Hub orb shows dark two-tone gradient (#1C1C1E → #2C2C30)
- Hovering a pod orb intensifies its glow from 0.25 to 0.40 opacity
</verification>

<success_criteria>
- Two-tone gradient visible on all orbs (not flat fill)
- Glow halos colored per pod, reactive on hover
- Health ring SVG renders when healthPercent prop is provided
- Hub orb has dark two-tone gradient, no hover scale
- All existing orb functionality preserved (click, drag, animation)
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-design-implementation/02.1-02-SUMMARY.md`
</output>
