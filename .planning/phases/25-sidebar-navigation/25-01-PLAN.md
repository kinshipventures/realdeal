---
phase: 25-sidebar-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/nav/Sidebar.tsx
  - src/App.tsx
autonomous: false
requirements:
  - NAV-SIDEBAR
must_haves:
  truths:
    - "Desktop shows a collapsible left sidebar instead of the floating bottom pill"
    - "Sidebar has grouped sections: Map, Core (Pulse/Contacts/Pipelines/Projects), Pods sub-nav, Utilities (Search/Sign out/Demo)"
    - "Sidebar collapses to 56px icon-only, expands to 220px with labels"
    - "Collapsed state persists across page reloads via localStorage"
    - "/ renders OrbMap, /pulse renders Dashboard"
    - "Mobile bottom tab bar is unchanged except Pulse navigates to /pulse"
    - "Pods list appears in sidebar fetched from getPods()"
  artifacts:
    - path: "src/components/nav/Sidebar.tsx"
      provides: "Desktop collapsible sidebar component"
      min_lines: 100
    - path: "src/App.tsx"
      provides: "Updated routes (/ = OrbMap, /pulse = Dashboard) and layout (paddingLeft for sidebar)"
  key_links:
    - from: "src/components/nav/Sidebar.tsx"
      to: "src/lib/supabase-data.ts"
      via: "getPods() call in useEffect"
      pattern: "getPods\\(\\)\\.then"
    - from: "src/App.tsx"
      to: "src/components/nav/Sidebar.tsx"
      via: "Sidebar rendered in AppShell for desktop"
      pattern: "import.*Sidebar.*from.*nav/Sidebar"
    - from: "src/App.tsx"
      to: "routes"
      via: "index route renders OrbMap, /pulse renders Dashboard"
      pattern: "Route index element=.*OrbMap"
---

<objective>
Replace the floating bottom pill navigator with a collapsible left sidebar on desktop. Swap routes so `/` renders OrbMap (map as home) and `/pulse` renders Dashboard.

Purpose: Map is the conceptual home of the app. Sidebar provides persistent navigation with pods sub-nav, matching Linear-style UX.
Output: Working sidebar on desktop, updated routes, mobile nav unchanged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-sidebar-navigation/25-CONTEXT.md
@.planning/phases/25-sidebar-navigation/25-RESEARCH.md

@src/App.tsx
@src/index.css
@src/lib/supabase-data.ts
@src/lib/types.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/types.ts:
```typescript
export interface Pod {
  id: string
  name: string
  color: HexColor | null
  owner: Owner | null
  is_priority: boolean
  cadence: Cadence | null
  description: string | null
  capacity: number | null
  enrichment_opt_in: boolean
  created_at: string
}
```

From src/lib/supabase-data.ts:
```typescript
export function getPods(): Promise<Pod[]>
```

From src/lib/sampleData.ts:
```typescript
export function isDemoMode(): boolean
export function setDemoMode(on: boolean): void
```

From src/integrations/supabase/client.ts:
```typescript
export const supabase: SupabaseClient
```

CSS tokens (src/index.css):
```css
--nav-bg: rgba(255,255,255,0.92);       /* light */
--nav-bg: rgba(30,30,30,0.85);          /* dark */
--edge-strong: rgba(0,0,0,0.12);
--tint-hover: rgba(0,0,0,0.06);
--divider: rgba(0,0,0,0.04);
--color-text-primary: rgba(0,0,0,0.82);
--color-text-secondary: rgba(0,0,0,0.45);
--color-text-tertiary: rgba(0,0,0,0.28);
--color-surface: #FFFFFF;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sidebar component and update AppShell layout + routes</name>
  <files>src/components/nav/Sidebar.tsx, src/App.tsx</files>
  <read_first>
    - src/App.tsx (current nav implementation, route definitions, AppShell layout, demo toggle, FAB, useIsMobile hook)
    - src/index.css (CSS tokens: --nav-bg, --edge-strong, --tint-hover, --divider, --color-text-*)
    - src/lib/supabase-data.ts (getPods function signature and cache pattern)
    - src/lib/types.ts (Pod interface shape)
    - src/lib/sampleData.ts (isDemoMode, setDemoMode exports)
    - .planning/phases/25-sidebar-navigation/25-RESEARCH.md (patterns, anti-patterns, pitfalls)
  </read_first>
  <action>
**Create `src/components/nav/Sidebar.tsx`** (per D-01, D-02, D-05, D-06):

New file. Desktop-only collapsible left sidebar, Linear-style.

Props interface:
```typescript
interface SidebarProps {
  onSearch: () => void
  demo: boolean
  onDemoToggle: () => void
}
```

Structure per D-05 - visually grouped sections separated by 1px dividers:
1. **Map** (top, primary) - globe/network icon, navigates to `/`, active when pathname is `/` or `/map`
2. **Divider**
3. **Core section** - Pulse (`/pulse`), Contacts (`/contacts`), Pipelines (`/pipelines`), Projects (`/projects`)
4. **Divider**
5. **Pods sub-nav** - "Pods" section heading with chevron toggle. Fetch pods via `getPods().then(setPods)` in useEffect with empty deps. Render pod names as nav items linking to `/pod/:id`. Show 8px color dot (pod.color) as icon. Hide pods section entirely when sidebar is collapsed. Pods list section gets `overflowY: auto`, `maxHeight: 200px` for scroll on long lists. Persist `podsOpen` state to localStorage key `realdeal:sidebar-pods-open`.
6. **Spacer** (flex: 1 to push utilities to bottom)
7. **Utilities at bottom** - Search icon button (triggers `onSearch` prop, per D-06 recommendation: icon button not input field), Demo toggle row, Sign out button

Sidebar container (per D-02 frosted glass):
- `position: fixed`, `top: 0`, `left: 0`, `bottom: 0`
- `width`: 56px collapsed, 220px expanded (per D-01)
- `background: var(--nav-bg)`, `backdropFilter: blur(20px)`, `WebkitBackdropFilter: blur(20px)`
- `borderRight: 1px solid var(--edge-strong)`
- `display: flex`, `flexDirection: column`
- `zIndex: 100`, `overflow: hidden`
- `transition: width 0.2s cubic-bezier(0.215, 0.61, 0.355, 1)`

Nav item pattern:
- `display: flex`, `alignItems: center`, `gap: 10`, `width: 100%`
- `padding: 8px 16px`, `borderRadius: 8`, `border: none`, `cursor: pointer`
- Active: `background: var(--tint-hover)`, `fontWeight: 600`, `color: var(--color-text-primary)`
- Inactive: `background: transparent`, `fontWeight: 500`, `color: var(--color-text-secondary)`
- Icon container: `width: 20`, `flexShrink: 0`, `display: flex`, `justifyContent: center`
- Label: `opacity: collapsed ? 0 : 1`, `transition: opacity 0.15s`, `whiteSpace: nowrap`, `overflow: hidden`, `fontSize: 13`
- Use `aria-current="page"` for active state

Collapse toggle (per research Pattern 7):
- Chevron button at the bottom of the sidebar, above utilities
- Points right when collapsed, left when expanded
- Click-only, no hover-expand
- Persist to `localStorage` key `realdeal:sidebar-collapsed` (value `'1'` or `'0'`)

SVG icons: Use inline SVGs matching the style of existing nav icons (stroke-based, strokeWidth 1.5, 20x20 viewBox 0 0 24 24). Icons needed:
- Map: globe/network (reuse existing map icon SVG from AppShell mobile nav)
- Pulse: heart (reuse existing from AppShell)
- Contacts: list (reuse existing)
- Pipelines: bar chart (reuse existing)
- Projects: folder (reuse existing)
- Search: magnifying glass (reuse existing)
- Sign out: log-out arrow (reuse existing)
- Collapse toggle: chevron-left / chevron-right
- Pods section: chevron-down / chevron-right for expand/collapse

Sign out handler: `async () => { await supabase.auth.signOut(); window.location.href = '/login' }` (same as current)

Demo toggle: Same visual as current but inline row format. Show icon (beaker or toggle) + "Demo" label. Brand-tinted border/text when active per current pattern.

**Update `src/App.tsx`**:

1. **Route changes** (per D-09, D-10, D-11):
   - Change `<Route index element={<Dashboard />} />` to `<Route index element={<OrbMap />} />`
   - Add `<Route path="pulse" element={<Dashboard />} />`
   - Keep `<Route path="map" element={<OrbMap />} />` for backward compat / mobile nav
   - Keep `<Route path="pulse/nurturing" element={<NurturingHub />} />` as-is (already absolute path)

2. **Route detection flags** (pitfall 1 fix):
   - Change `isMap` to: `location.pathname === '/' || location.pathname === '/map'`
   - Change `isPulse` to: `location.pathname === '/pulse' || location.pathname.startsWith('/pulse/')`
   - Remove the complex negative-condition isPulse logic

3. **Mobile nav updates** (per D-07, pitfall 2 fix):
   - Mobile Pulse button: change `navigate('/')` to `navigate('/pulse')`
   - Mobile Map button: keep `navigate('/map')` as-is
   - Mobile tab bar otherwise unchanged

4. **Desktop layout** (per D-01):
   - Import `Sidebar` from `@/components/nav/Sidebar`
   - Add `sidebarWidth` state: derive from collapsed state (read localStorage `realdeal:sidebar-collapsed` to get initial collapsed value, then `collapsed ? 56 : 220`)
   - Actually: pass a `onWidthChange` callback to Sidebar, or lift collapsed state to AppShell. Simpler: lift `collapsed` state to AppShell so the layout div knows the width.
   - Replace `paddingBottom: isMobile ? 56 : 0` with:
     ```
     paddingLeft: isMobile ? 0 : (collapsed ? 56 : 220),
     paddingBottom: isMobile ? 56 : 0,
     transition: isMobile ? undefined : 'padding-left 0.2s cubic-bezier(0.215, 0.61, 0.355, 1)',
     ```
   - Render `{!isMobile && <Sidebar ... />}` inside AppShell, before the layout div
   - Pass `onSearch={() => setShowSearch(true)}`, `demo`, and `onDemoToggle` handler to Sidebar
   - `collapsed` and `setCollapsed` as props to Sidebar (since AppShell needs the width)

5. **Remove desktop pill nav**: Delete the entire `else` branch (desktop floating pill navigator, lines ~229-396 in current App.tsx)

6. **Move demo toggle**: Remove the fixed-position demo toggle button from AppShell (lines ~447-476). It moves into Sidebar for desktop. For mobile, keep a simplified version or remove (demo toggle is a dev tool). Actually per D-05, demo toggle goes in sidebar utilities. For mobile, leave the existing fixed demo toggle since sidebar doesn't render on mobile. Keep the mobile demo toggle as-is.

7. **FAB position**: Keep `right: 24`. No sidebar adjustment needed (FAB is right-anchored, sidebar is left per research Pattern 8). Update `bottom` for desktop: change from `84` (which accounted for pill nav) to `24` since there's no bottom pill anymore. Mobile stays `bottom: 72`.
  </action>
  <verify>
    <automated>cd /Users/gabrielmurray/dev/realdeal && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/nav/Sidebar.tsx exists and is > 100 lines
    - src/components/nav/Sidebar.tsx contains `position: 'fixed'`
    - src/components/nav/Sidebar.tsx contains `getPods().then`
    - src/components/nav/Sidebar.tsx contains `realdeal:sidebar-collapsed`
    - src/components/nav/Sidebar.tsx contains `backdropFilter`
    - src/components/nav/Sidebar.tsx contains `onSearch`
    - src/components/nav/Sidebar.tsx contains `'/pulse'`
    - src/components/nav/Sidebar.tsx contains `'/pipelines'`
    - src/components/nav/Sidebar.tsx contains `'/projects'`
    - src/components/nav/Sidebar.tsx contains `'/contacts'`
    - src/components/nav/Sidebar.tsx contains `pod.color`
    - src/components/nav/Sidebar.tsx contains `supabase.auth.signOut`
    - src/App.tsx contains `Route index element={<OrbMap`
    - src/App.tsx contains `path="pulse" element={<Dashboard`
    - src/App.tsx contains `import { Sidebar }` or `import { Sidebar` from nav/Sidebar
    - src/App.tsx contains `paddingLeft` (sidebar layout shift)
    - src/App.tsx mobile Pulse button contains `navigate('/pulse')` not `navigate('/')`
    - src/App.tsx does NOT contain the floating pill nav (no `borderRadius: 100` on a nav element with `position: 'fixed'` and `bottom: 16`)
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>
    Desktop renders a collapsible left sidebar with Map/Pulse/Contacts/Pipelines/Projects nav, pods sub-nav, search/signout/demo utilities. / loads OrbMap, /pulse loads Dashboard. Mobile tab bar unchanged except Pulse points to /pulse. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify sidebar navigation and route changes</name>
  <what-built>Collapsible left sidebar replacing the floating pill nav on desktop. Route swap: / is now OrbMap, /pulse is Dashboard. Mobile tab bar updated.</what-built>
  <how-to-verify>
    1. Open http://localhost:5173/ - should show OrbMap (not Dashboard)
    2. Open http://localhost:5173/pulse - should show Dashboard
    3. Verify sidebar is visible on the left with sections: Map (top), Core (Pulse/Contacts/Pipelines/Projects), Pods list, Utilities (Search/Sign out/Demo) at bottom
    4. Click the collapse toggle - sidebar should shrink to icon-only (56px). Labels fade out.
    5. Refresh page - sidebar should remain collapsed (localStorage persistence)
    6. Expand sidebar - click a pod name in the Pods section - should navigate to /pod/:id
    7. Click Search in sidebar - should open the Cmd+K search palette
    8. Click Demo toggle - should swap to demo data
    9. Resize to mobile width (< 767px) - should see bottom tab bar, no sidebar
    10. On mobile, tap Pulse - should go to /pulse (Dashboard), not /
    11. Navigate to /pulse/nurturing - should load NurturingHub
    12. Verify no floating pill nav remains on desktop
    13. Verify FAB (green + button) appears correctly on non-map pages
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` passes with zero TypeScript errors
- Desktop: sidebar visible, collapsible, all nav items work
- Mobile: bottom tab bar intact, Pulse navigates to /pulse
- Routes: / = OrbMap, /pulse = Dashboard, /pulse/nurturing = NurturingHub
- Pods sub-nav populated from data layer
- No floating pill nav on desktop
</verification>

<success_criteria>
- Sidebar renders on desktop with frosted glass treatment
- Sidebar collapses to 56px / expands to 220px with smooth transition
- Collapsed state persists in localStorage
- All 5 core nav items + pods sub-nav + 3 utility items present
- Route swap works: / = OrbMap, /pulse = Dashboard
- Mobile bottom tab bar unchanged except /pulse path
- Build passes, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-sidebar-navigation/25-01-SUMMARY.md`
</output>
